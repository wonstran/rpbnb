% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residuals.R
\name{residuals.rpbnb_fit}
\alias{residuals.rpbnb_fit}
\title{Residuals for a random-parameter bivariate NB model}
\usage{
\method{residuals}{rpbnb_fit}(
  object,
  type = c("quantile", "pearson", "deviance", "response"),
  margin = c("both", "y1", "y2"),
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{rpbnb_fit} object from \code{\link[=fit_rpbnb]{fit_rpbnb()}}.}

\item{type}{Residual type: \code{"quantile"} (default), \code{"pearson"}, or
\code{"response"}. \code{"deviance"} is not supported for \code{rpbnb_fit}.}

\item{margin}{Which margin: \code{"both"} (default), \code{"y1"}, or \code{"y2"}.}

\item{seed}{Optional integer seed for the quantile-residual randomization;
does not disturb the caller's RNG stream.}

\item{...}{Unused.}
}
\value{
A numeric vector for a single margin, or a two-column data frame
(\code{y1}, \code{y2}) for \code{margin = "both"}.
}
\description{
Per-margin residuals for an \code{\link[=fit_rpbnb]{fit_rpbnb()}} model. Each margin is a mixture of
NB2 distributions over the random-coefficient draws. \code{"quantile"} returns
randomized quantile residuals (Dunn & Smyth 1996) from the exact mixture
predictive CDF (the recommended residual); \code{"pearson"} uses the exact mixture
marginal variance. \code{"deviance"} is not defined for the mixture and errors.
}
