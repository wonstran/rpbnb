% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{bnb_residual_checks}
\alias{bnb_residual_checks}
\title{Residual checks for a bivariate NB model}
\usage{
bnb_residual_checks(
  fit,
  seed = NULL,
  outlier_z = 3,
  digits = 4,
  print_output = TRUE
)
}
\arguments{
\item{fit}{A \code{bnb_fit} or \code{rpbnb_fit} object.}

\item{seed}{Optional integer seed for the RQR randomization.}

\item{outlier_z}{Absolute-RQR threshold flagging an outlier (default 3).}

\item{digits}{Decimal places for printed output.}

\item{print_output}{Logical; if \code{FALSE}, suppress printing.}
}
\value{
Invisibly, an object of class \code{bnb_residual_checks}.
}
\description{
Formal residual diagnostics for a \code{\link[=fit_bnb]{fit_bnb()}} or \code{\link[=fit_rpbnb]{fit_rpbnb()}} fit: a
normality test on the randomized quantile residuals (Shapiro-Wilk for
\code{n <= 5000}, else Kolmogorov-Smirnov vs N(0,1)); the NB2 dispersion statistic
(\code{sum(pearson^2) / (n - k)}) per margin; the cross-margin RQR correlation (a
check that the Famoye/copula dependence captured the association); an outlier
count/index list (\verb{|RQR| > outlier_z}); and a composite misspecification
verdict combining these signals.
}
