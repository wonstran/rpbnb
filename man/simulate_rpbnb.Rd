% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_rpbnb.R
\name{simulate_rpbnb}
\alias{simulate_rpbnb}
\title{Simulate data from a random-parameter bivariate NB process}
\usage{
simulate_rpbnb(
  n,
  beta1,
  beta2,
  random_1 = NULL,
  random_2 = NULL,
  dispersion = c(m1 = 0.5, m2 = 0.5),
  lambda = 0,
  covariates = NULL,
  seed = 1234
)
}
\arguments{
\item{n}{Number of observations.}

\item{beta1, beta2}{Named numeric vectors of fixed coefficient means per
equation; must include "(Intercept)".}

\item{random_1, random_2}{Named lists giving random coefficients, e.g.
\code{list(x1 = list(sd = 0.5))}. Means come from \code{beta1}/\code{beta2}.}

\item{dispersion}{Named numeric \code{c(m1 = ..., m2 = ...)} NB2 dispersions.}

\item{lambda}{Famoye dependence parameter (0 = independent margins).}

\item{covariates}{Optional data frame of covariates; if NULL, standard-normal
columns are generated for every non-intercept name.}

\item{seed}{Random seed.}
}
\value{
A list with \code{data} (y1, y2, covariates), \code{coef_realized}
(per-obs coefficients per equation), \code{mu} (conditional means), \code{true}
(parameters), \code{settings}, and \code{meta} (R/seed/timestamp passed by caller).
}
\description{
Simulate data from a random-parameter bivariate NB process
}
\examples{
sim <- simulate_rpbnb(n = 500,
  beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
  beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
  random_1 = list(x1 = list(sd = 0.5)),
  dispersion = c(m1 = 0.4, m2 = 0.5), seed = 1)
head(sim$data)
}
