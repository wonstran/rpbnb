% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{rpbnb_marginal_effects}
\alias{rpbnb_marginal_effects}
\title{Marginal effects for a random-parameter bivariate NB model}
\usage{
rpbnb_marginal_effects(
  fit,
  which = c("y1", "y2", "both", "all"),
  type = c("AME", "MEM"),
  vars = NULL,
  include_intercept = FALSE,
  digits = 4,
  print_output = TRUE,
  n_cores = 1L,
  scaling = NULL,
  log_vars = NULL
)
}
\arguments{
\item{fit}{An \code{rpbnb_fit} object from \code{\link[=fit_rpbnb]{fit_rpbnb()}}.}

\item{which}{Which margin(s): "y1", "y2", "both", or "all".}

\item{type}{"AME" (average over the sample) or "MEM" (effect at the mean row).}

\item{vars}{Optional variable names or indices to restrict output.}

\item{include_intercept}{Logical; include the intercept term.}

\item{digits}{Number of decimal places for printed output.}

\item{print_output}{Logical; if \code{FALSE}, suppress printing.}

\item{n_cores}{Number of worker processes for the delta-method standard-error
jacobian (1 = sequential, the default). When \code{n_cores > 1}, the jacobian's
independent per-parameter columns are dispatched across a
\code{parallel::makeCluster()} cluster (one cluster per call, shared across
every equation \code{which} computes); results are numerically identical to the
sequential path. Falls back to sequential with a warning if the \code{parallel}
package is unavailable.}

\item{scaling}{Optional named list of \code{c(center =, scale =)} pairs, one per
covariate that was centred and/or scaled before fitting (e.g. via
\code{rpbnb(standardize = TRUE)}, whose \verb{$scaling} field is exactly this
shape), used to report the results in the covariate's original units.
Columns not named are left alone, so binary indicators and untransformed
variables need no entry.

Only the reported quantity is rescaled; the fitted design is not rebuilt.
That distinction is not cosmetic when a standardized covariate also
carries a random coefficient: the random term enters as \code{x_std * dev}, so
substituting \code{x_raw = x_std * s + c} would add a \code{(c/s) * dev} random
intercept the model never estimated -- the disguised random intercept
centring was meant to remove, back again. The chain rule avoids that:
\code{d(mu)/d(x_raw) = (d(mu)/d(x_std))/s}, and the elasticity's leading factor
becomes \code{x_raw/s = x_std + c/s}. Binary effects are untouched.}

\item{log_vars}{Optional character vector naming covariates that are
ALREADY a log, so results are reported per unit of the underlying
variable \code{v_j = exp(x_j)} rather than per unit of \code{x_j} itself. Without
this the elasticity formula treats \verb{log v} as an ordinary regressor and
returns \code{xbar * b}, the elasticity with respect to the LOG -- for a
log-traffic column that can be an order of magnitude off from the
elasticity with respect to traffic itself (the coefficient). Rejects
binary columns (a 0/1 indicator is not a log-transformed variable).}
}
\value{
A data frame (single margin, invisibly) or a named list of data frames
(\code{both}/\code{all}), each with columns \code{Name}, \code{Estimate}, \code{StdErr}, \code{z}, \code{p},
\code{Signif}, \code{var_type}.
}
\description{
Average marginal effects (AME) or marginal effects at the mean (MEM) for each
margin of an \code{\link[=fit_rpbnb]{fit_rpbnb()}} fit, built on the Monte-Carlo integrated population
mean \eqn{\mu_i = E_\beta[\exp(x_i'\beta)]} (the same estimand as
\code{predict.rpbnb_fit()}, reusing the fit's stored draws). Continuous effects are
\eqn{\partial \mu_i/\partial x_{ij} = \mathrm{mean}_r\, \mathrm{coef}_{rj}
\exp(\mathrm{lp}_{ir})} (the realized coefficient per draw, so random and fixed
columns are handled uniformly); binary (0/1) effects are the integrated
discrete difference \eqn{E[Y|x_j=1] - E[Y|x_j=0]}. Standard errors use a
numeric delta method over the equation's mean and log-scale parameters.
}
\details{
For a model fitted with an \code{offset()}, absolute marginal effects scale with
the offset-inclusive mean: AME uses each observation's training offset, and
MEM uses the mean training offset.
}
\examples{
sim <- simulate_rpbnb(n = 400,
  beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
  beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
  random_1 = list(x1 = list(sd = 0.5)),
  dispersion = c(m1 = 0.4, m2 = 0.5), seed = 1)
fit <- fit_rpbnb(y1 ~ x1, y2 ~ x1, data = sim$data, random_1 = "x1",
                 draws = 100)
rpbnb_marginal_effects(fit, which = "y1", type = "AME")
}
\seealso{
\code{\link[=bnb_marginal_effects]{bnb_marginal_effects()}} for fixed-coefficient \code{bnb_fit} models.
}
