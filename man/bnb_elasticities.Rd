% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{bnb_elasticities}
\alias{bnb_elasticities}
\title{Elasticities and semi-elasticities for a bivariate NB model}
\usage{
bnb_elasticities(
  fit,
  which = c("y1", "y2", "both"),
  type = c("AME", "MEM"),
  vars = NULL,
  include_intercept = FALSE,
  digits = 4,
  print_output = TRUE
)
}
\arguments{
\item{fit}{A \code{bnb_fit} object from \code{\link[=fit_bnb]{fit_bnb()}}.}

\item{which}{Which margin(s): "y1", "y2", or "both".}

\item{type}{"AME" (uses sample mean of x) or "MEM" (uses mean design row).}

\item{vars}{Optional variable names to restrict output.}

\item{include_intercept}{Logical; include the intercept term.}

\item{digits}{Number of decimal places for printed output.}

\item{print_output}{Logical; if \code{FALSE}, suppress printing.}
}
\value{
A data frame (single margin) or a named list of data frames (both),
each with columns \code{Name}, \code{Estimate}, \code{StdErr}, \code{z}, \code{p}, \code{Signif},
\code{var_type}.
}
\description{
For continuous regressors, the elasticity is \eqn{\beta_j E[x_j]}; for
binary (0/1) regressors, the semi-elasticity is \eqn{\exp(\beta_j) - 1}
(proportional change moving from 0 to 1).
}
\examples{
d <- read.csv(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb"))
fit <- fit_bnb(docvis ~ outwork + age, hospvis ~ outwork, data = d,
               dependence = "famoye")
bnb_elasticities(fit, which = "both", type = "AME")
}
