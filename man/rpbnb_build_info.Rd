% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_info.R
\name{rpbnb_build_info}
\alias{rpbnb_build_info}
\title{Report how this installation of rpbnb was compiled}
\usage{
rpbnb_build_info()
}
\value{
A list, invisibly when printed:
\describe{
\item{\code{optimized}}{\code{TRUE} when compiled at \code{-O1} or above, read from
the compiler's own \verb{__OPTIMIZE__} macro rather than inferred.}
\item{\code{openmp}}{\code{TRUE} when OpenMP is available, so \code{n_cores > 1} can
do anything. A build without it fits single-threaded whatever
\code{control$n_cores} says.}
\item{\code{openmp_max_threads}}{The OpenMP runtime's own thread ceiling.}
\item{\code{assertions_enabled}}{\code{TRUE} when \code{NDEBUG} is unset, a further
marker of a debug build.}
\item{\code{compiler}}{Compiler and version that built the shared object.}
}
}
\description{
Nearly all of this package's running time is compiled likelihood
evaluation, so whether its shared object was built with optimization is a
performance fact worth roughly a factor of two on every fit -- and nothing
in R's own output tells you which build you have. This reports what the
compiler actually did.
}
\details{
A source install (\code{install.packages(type = "source")}, \verb{R CMD INSTALL},
\code{remotes::install_github()}) compiles with R's own \code{CXXFLAGS}, which is
\code{-O2} on every standard platform, so the optimized build is what you get by
default. The package does not -- and by CRAN policy must not -- override
those flags. Two things do produce a slow build: a \code{-O0} (or \code{-Og}) entry
in the user's \verb{~/.R/Makevars}, which applies to every package compiled on
that machine, and development helpers that inject debug flags of their own,
notably \code{pkgbuild::compile_dll(debug = TRUE)} -- its default -- which
\code{devtools::load_all()} uses when recompiling changed sources.
}
\examples{
rpbnb_build_info()
}
