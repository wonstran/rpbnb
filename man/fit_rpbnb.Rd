% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rpbnb.R
\name{fit_rpbnb}
\alias{fit_rpbnb}
\title{Fit a bivariate random-parameter negative binomial model}
\usage{
fit_rpbnb(
  formula_1,
  formula_2,
  data,
  random_1 = NULL,
  random_2 = NULL,
  draws = 400,
  draw_type = "halton",
  seed = 1234,
  start = NULL,
  control = rpbnb_control(),
  dependence = "famoye",
  poisson_1 = FALSE,
  poisson_2 = FALSE
)
}
\arguments{
\item{formula_1, formula_2}{Model formulas for the two count outcomes.}

\item{data}{A data frame.}

\item{random_1, random_2}{Random coefficients per equation. Either a character
vector of \code{model.matrix} column names (all Normal), or a named list whose
values are a distribution name (\code{"normal"}, \code{"lognormal"}, \code{"uniform"},
\code{"triangular"}) or a list \code{list(dist = ..., sign = ...)} (\code{sign} is -1/1 and
lognormal-only). NULL means all-fixed for that equation.}

\item{draws}{Number of simulation draws for the optimization.}

\item{draw_type}{Quasi-random draw type. Only "halton" is supported in this version.}

\item{seed}{Random seed for the simulation draws.}

\item{start}{Optional starting parameter vector.}

\item{control}{An \code{\link[=rpbnb_control]{rpbnb_control()}} object. Estimation uses BFGS, the only
optimizer \code{control$method} accepts.}

\item{dependence}{Dependence structure: "famoye" (default; Famoye/Sarmanov)
or an \code{\link[=copula]{copula()}} object for copula dependence (Frank / Gaussian /
Clayton). Both paths use the multithreaded (OpenMP) C++ simulated
likelihood; the copula path is more numerically expensive per evaluation
(discrete-copula pmf + per-draw NB CDF corners), so fits typically take
noticeably longer than the Famoye path at comparable \code{draws}/\code{n}. Random
coefficients on 0/1 dummy regressors are weakly identified under the
copula path (NB dispersion trades off against the random-coefficient
scale); prefer random coefficients on continuous regressors.}

\item{poisson_1, poisson_2}{Fit the corresponding margin at its Poisson limit
(NB2 dispersion \code{m = 0}): the margin's \code{log_m} is pinned at a tiny constant
and held fixed, so it is not a free parameter and the fit is a properly
nested restriction of the NB model. Pair with \code{\link[=lr_test]{lr_test()}} (\code{boundary = TRUE}) to test a margin for overdispersion (\code{H0: m = 0}). This is a
numerical m->0 limit, accurate to well under one log-likelihood unit, not
an exact Poisson reparameterization. Not supported with a \code{\link[=copula]{copula()}}
dependence.}
}
\value{
An object of class \code{rpbnb_fit}.
}
\description{
Maximum simulated likelihood estimation with normal random coefficients and
Famoye/Sarmanov dependence. Random coefficients are selected per equation by
name via \code{random_1} / \code{random_2}.
}
\examples{
sim <- simulate_rpbnb(n = 600,
  beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
  beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
  random_1 = list(x1 = list(sd = 0.5)),
  dispersion = c(m1 = 0.4, m2 = 0.5), seed = 1)
fit <- fit_rpbnb(y1 ~ x1, y2 ~ x1, data = sim$data, random_1 = "x1",
                 draws = 100, control = rpbnb_control(compute_se = FALSE))
coef(fit)

\donttest{
# Copula dependence instead of Famoye/Sarmanov (slower; fewer draws here
# for a quick example -- use more in practice)
sim_cop <- simulate_rpbnb_copula(n = 600,
  beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
  beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
  random_1 = list(x1 = list(sd = 0.3)),
  dispersion = c(m1 = 0.4, m2 = 0.5),
  copula = copula("normal", par = 0.5), seed = 1)
fit_cop <- fit_rpbnb(y1 ~ x1, y2 ~ x1, data = sim_cop$data, random_1 = "x1",
                     dependence = copula("normal"), draws = 100,
                     control = rpbnb_control(compute_se = FALSE))
tanh(coef(fit_cop)[["z_theta"]])  # estimated copula rho
}
}
