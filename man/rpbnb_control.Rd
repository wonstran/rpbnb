% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{rpbnb_control}
\alias{rpbnb_control}
\title{Control parameters for every rpbnb estimator}
\usage{
rpbnb_control(
  method = c("BFGS"),
  iterlim = NULL,
  reltol = 1e-08,
  print_level = NULL,
  draws_hessian = 100L,
  halton_burn = 300L,
  n_cores = 1L,
  compute_se = TRUE,
  hessian = c("numeric", "analytic"),
  se_method = NULL,
  hess_eps = 1e-05,
  hess_r = 4L,
  gradtol = 1e-05,
  restarts = 10L,
  max_threads = NULL,
  max_workload = NULL,
  parallel_tape = TRUE,
  tape_chunks = NULL
)
}
\arguments{
\item{method}{Optimizer used by the \code{maxLik} fitters. Only "BFGS" is
implemented and wired through; it is the sole accepted value.}

\item{iterlim}{Maximum optimizer iterations. \code{NULL} (default) uses 300 for
the \code{maxLik} fitters and 500 for the TMB engine's \code{nlminb}.}

\item{reltol}{Relative convergence tolerance.}

\item{print_level}{Optimizer verbosity, and the switch that silences the
boundary-test progress messages. \code{NULL} (default) uses 2 under \code{maxLik}
and 1 under the TMB engine. Under TMB it drives two separate switches --
\code{MakeADFun(silent = print_level == 0)} and \code{nlminb}'s
\code{trace = max(0, print_level - 1)} -- so the levels are:
\describe{
\item{\code{0}}{Silent (the pre-0.4.6 TMB default).}
\item{\code{1}}{TMB's own output only: an \verb{outer mgc:} line per outer
evaluation, plus the one-time tape/atomic construction on the first
fit of a session. No \code{nlminb} trace.}
\item{\code{2}}{Adds \code{nlminb}'s per-iteration objective and parameter
vector -- the lowest level that traces every iteration.}
\item{\verb{>2}}{\code{nlminb}'s \code{trace} is a print \emph{interval}, not a verbosity
level, so higher values print the objective \emph{less} often.}
}
Note that \code{rpbnb_tmb_boundary_tests()} builds its own control when one is
not supplied, so its restricted refits print at their own default
regardless of this setting.}

\item{draws_hessian}{Retained for backward compatibility but unused by every
estimator: the random-parameter numeric Hessian is taken with the same
optimization draws that produced the estimate (same-draw curvature), so it
no longer resimulates a separate Hessian draw set. Supplying it is always
reported as an ignored setting.}

\item{halton_burn}{Number of leading Halton points discarded before forming
the simulation draws.}

\item{n_cores}{Worker processes for \code{\link[=fit_rpbnb]{fit_rpbnb()}}'s optional cluster path, or
OpenMP threads for \code{\link[=fit_rpbnb_tmb]{fit_rpbnb_tmb()}} (1 = sequential in both cases). Under
the TMB engine the \emph{realized} thread count -- after \code{max_threads} and
hardware capping -- is also what \code{max_workload}/\code{tape_chunks} size the
tape against when \code{parallel_tape = TRUE}; see "How \code{n_cores},
\code{max_workload}, and \code{tape_chunks} interact" below.}

\item{compute_se}{If FALSE, skip the Hessian and standard errors. The TMB
engine has its own \code{inference} argument for this instead.}

\item{hessian}{How \code{\link[=fit_bnb]{fit_bnb()}} (famoye) computes the Hessian for standard
errors: "numeric" (default, \code{\link[numDeriv:hessian]{numDeriv::hessian()}}) or "analytic" (the
closed-form Famoye (2010) Appendix Hessian). Both freeze the lambda-bounds
at the optimum and yield the same observed-information SEs.}

\item{se_method}{Standard-error method for \code{\link[=fit_rpbnb]{fit_rpbnb()}} (the random-parameter
model): "numeric" uses the \code{\link[numDeriv:hessian]{numDeriv::hessian()}} observed-information
Hessian; "analytic" uses the closed-form observed-information Hessian
(Famoye (2010) per-draw second derivatives assembled via the Louis
mixture formula) -- exact and much faster than "numeric" for larger models;
"opg" uses the BHHH / outer-product-of-gradients information from the
per-observation scores -- fastest (one gradient pass, versus "numeric"'s
O(npar^2) finite-difference log-likelihood evaluations), but relies on the
information-matrix equality so it is unreliable for parameters at a
boundary (e.g. a random-coefficient SD estimated near 0). For copula
dependence (\code{\link[=fit_rpbnb]{fit_rpbnb()}} with \code{dependence = copula(...)}), only "opg" and
"numeric" are available; "analytic" is not implemented for the copula path
and errors.

Default \code{NULL}, meaning "this dependence family's own default", resolved
once the fit is dispatched (same NULL-until-resolved pattern as
\code{iterlim}/\code{print_level}): "numeric" for \code{dependence = "famoye"}, "opg" for
\code{dependence = copula(...)}. The two differ because their speed/reliability
tradeoffs differ -- Famoye's fast, exact "analytic" Hessian makes
"numeric" (not "opg") the conservative default there, where the copula
path has no analytic Hessian at all and its own numeric Hessian is
markedly slower than OPG (measured ~3x on an 11-parameter fit) for SEs
that mostly agree with it anyway, except (per the boundary caveat above)
on random-coefficient scale parameters -- fall back to "numeric" for just
those, or use \code{\link[=rpbnb_boundary_tests]{rpbnb_boundary_tests()}}, if precise inference on an SD
estimated near 0 matters. Read back a resolved fit's own
\code{fit$control$se_method} (or the value read off \code{object$control} reported
by \code{print()}/\code{summary()}) rather than assuming which default applied.}

\item{hess_eps, hess_r}{Step and Richardson order for \code{\link[numDeriv:hessian]{numDeriv::hessian()}}
(used only when the numeric Hessian is selected).}

\item{gradtol}{Stationarity tolerance for the TMB engine's score, applied
\emph{relative} to the objective: the fit is treated as stationary once
\code{max(abs(gradient))} falls below \code{gradtol * max(1, abs(nll))}.
\code{nlminb()} declares convergence from its relative function test,
which can fire while the score is still far from zero; a Hessian taken
there is not a curvature and its inverse can carry negative variances. The
fit is restarted until the gradient clears this tolerance; missing it is
not warned about on its own, because the copula families legitimately
optimise against a clamp or a probability floor where no step improves the
objective, but it is reported in the warning raised when the Hessian
actually fails to factor, and is kept on the fit as
\code{optimizer$max_abs_gradient}. The scaling matters because the score
is a sum over observations, so an absolute cutoff would tighten with
sample size for no statistical reason.}

\item{restarts}{Maximum number of times to restart \code{nlminb()} from its
own answer while \code{gradtol} is unmet. Restarting resets the trust
region and step scaling that stalled the first solve. Set to \code{0L} for
the single-call behaviour of earlier versions.}

\item{max_threads}{Maximum OpenMP threads permitted for one TMB fit.
\code{NULL} (default) means \code{n_cores}, so threads are not capped unless
set explicitly below \code{n_cores}.}

\item{max_workload}{Maximum weighted observation-draw evaluations permitted before TMB tape construction; \code{Inf} disables the guard. The default and every figure here are derived from \code{TAPE_CALIBRATION}, so this text cannot drift from the shipped behaviour.

With \code{parallel_tape = FALSE} the budget is per fit; with the default \code{parallel_tape = TRUE} the tapes are built concurrently and the guard multiplies the workload by the realized thread count (see \code{n_cores} above for how that count is realized).

One unit is one weighted observation-draw. All figures are measured by \code{inst/dev/tmb_benchmark_memory.R}, whose raw results are stored in \code{inst/extdata/memory_calibration.csv}.

Retained tape size depends on \code{n * draws} alone: tape (MiB) = 13.374 + 0.001164 * units, with R^2 = 0.9994 (1221 bytes per unit over the largest workloads; per-unit cost is higher at small workloads because of the fixed intercept).

The budget is set on \emph{peak} working set, not on retained tape, because peak is what exhausts memory: TMB records the whole likelihood before pruning it to each parallel region, so peak runs about 6.11 times the retained tape plus first-evaluation growth, or 12083 bytes per unit measured directly against peak. The default of 700,000 units therefore targets a peak of about 8 GiB for one fit.

Families cost different amounts per unit, so each carries a weight -- the largest \emph{peak} ratio to Famoye observed at matched workload, rounded up to the next tenth: independence 0.7, famoye 1, frank 3.6, gaussian 0.9, clayton 1.1. Frank peaks at over three and a half times Famoye, so treating it as unweighted would under-budget it more than threefold.

Raise \code{max_workload} deliberately against the memory you actually have, not to make one particular dataset fit.

As of \code{rpbnb_tmb_max_workload()}, the value \code{rpbnb_tmb_control()} actually uses by default is no longer the fixed figure above: it will auto-detect available memory and budget 80\% of it, falling back to the fixed 8 GiB figure only when detection is unavailable on the current platform. Call \code{\link{rpbnb_tmb_max_workload}} directly to set your own budget or detection fraction.}

\item{parallel_tape}{Construct per-thread TMB tapes concurrently. The
default \code{TRUE} builds them in parallel for faster tape
construction; objective and gradient evaluation remains parallel either
way. Set \code{FALSE} to build tapes sequentially instead and reduce
peak memory. Concurrent tape construction multiplies the per-tape
memory workload the \code{max_workload}/\code{tape_chunks} guard sizes
against by the realized thread count (see \code{max_workload} above);
pairing \code{parallel_tape = TRUE} with \code{max_workload = Inf}
disables that guard entirely; \code{rpbnb_control()} warns when it sees
that combination.}

\item{tape_chunks}{TMB engine, SML fits only. Number of draw chunks to
split \code{draws} into (see \code{draws} at \code{\link[=fit_rpbnb_tmb]{fit_rpbnb_tmb()}}).
\code{NULL} (default) auto-selects the smallest sufficient count when
the weighted workload exceeds \code{max_workload}, or \code{1L} (no
chunking) when it does not. Set explicitly to pin a layout regardless of
the auto-threshold; must not exceed \code{draws}. Chunking is exact for
the requested draws (not an approximation) at the cost of somewhat
slower gradient evaluations, and a chunked fit has no taped Hessian --
\code{confint(method = "profile")}/\code{rpbnb_tmb_dependence_profile()}
fall back to a Wald interval with a warning; Wald/optimHess inference
(the default) is unaffected. Ignored for \code{method = "laplace"}
(which has no draw dimension to chunk) and by every non-TMB estimator.}
}
\value{
An object of class \code{c("rpbnb_control", "rpbnb_tmb_control")} (a named
list). It carries both class names so that every historical
\code{inherits(control, ...)} check in the package and in user code accepts it.
}
\description{
One control object for all of the package's fitters -- \code{\link[=fit_bnb]{fit_bnb()}},
\code{\link[=fit_rpbnb]{fit_rpbnb()}}, \code{\link[=fit_rpbnb_tmb]{fit_rpbnb_tmb()}}, and \code{\link[=rpbnb]{rpbnb()}} with either \code{engine}. It
carries the union of the tuning knobs the estimators use; each fitter reads
the ones that apply to it and \strong{ignores the rest}, reporting the ignored
names in \code{print()}/\code{summary()} of the resulting fit rather than erroring.
That is what makes a script able to flip \code{engine = "classic"} to \code{"tmb"}
without rewriting its control call.
}
\section{Which parameters apply to which estimator}{
\tabular{llll}{
   Parameter \tab \code{\link[=fit_bnb]{fit_bnb()}} \tab \code{\link[=fit_rpbnb]{fit_rpbnb()}} \tab \code{\link[=fit_rpbnb_tmb]{fit_rpbnb_tmb()}} \cr
   \code{method}, \code{compute_se}, \code{hess_eps}, \code{hess_r} \tab yes \tab yes \tab ignored \cr
   \code{iterlim}, \code{reltol}, \code{print_level} \tab yes \tab yes \tab yes \cr
   \code{halton_burn}, \code{n_cores} \tab ignored \tab yes \tab yes \cr
   \code{hessian} \tab yes \tab ignored \tab ignored \cr
   \code{se_method} \tab ignored \tab yes \tab ignored \cr
   \code{gradtol}, \code{restarts}, \code{max_threads}, \code{max_workload}, \code{parallel_tape} \tab ignored \tab ignored \tab yes \cr
   \code{draws_hessian} \tab ignored \tab ignored \tab ignored \cr
}


Note that \code{iterlim} and \code{n_cores} mean different things to different
estimators -- a \code{maxLik} BFGS iteration limit versus an \code{nlminb} one, worker
\emph{processes} versus OpenMP \emph{threads}. They are not translated; each estimator
reads the number and applies its own meaning to it.
}

\section{Defaults that depend on the estimator}{


\code{iterlim} and \code{print_level} default to \code{NULL}, which means "this estimator's
own default": \code{iterlim} is 300 under \code{maxLik} and 500 under \code{nlminb}.
\code{print_level} is 2 under \code{maxLik} and, as of 0.4.6, 1 under the TMB engine
(it was 0 -- fully silent -- before that, which left a long TMB fit
printing nothing at all while it ran). The TMB default of 1 shows TMB's own
progress without \code{nlminb}'s per-iteration parameter vectors; see
\code{print_level} below for what each level prints. Supplying either explicitly
overrides that for every estimator; \code{print_level = 0} restores silence.
\code{max_threads} defaults to \code{n_cores} and \code{max_workload} is computed from
available memory by \code{\link[=rpbnb_tmb_max_workload]{rpbnb_tmb_max_workload()}} the first time a TMB fit
needs it (so a non-TMB fit never pays for the memory probe).
}

\section{How \code{n_cores}, \code{max_workload}, and \code{tape_chunks} interact (TMB, \code{method = "sml"})}{


These three only interact through \code{parallel_tape}. Before
\code{\link[=fit_rpbnb_tmb]{fit_rpbnb_tmb()}} builds a tape, it estimates a weighted workload:
\verb{nrow(data) * draws * <family weight> * (realized n_cores if parallel_tape = TRUE, else 1)}. \code{max_workload} caps that number, and
\code{tape_chunks} is what lets a fit exceed it anyway by building several
smaller tapes instead of one.
\itemize{
\item \code{n_cores} sets OpenMP threads for the objective/gradient, which are
always evaluated in parallel regardless of anything else here. With the
default \code{parallel_tape = TRUE}, the \emph{realized} thread count (after
\code{max_threads} and hardware capping) also multiplies the workload above,
because each thread builds its own full tape concurrently -- so raising
\code{n_cores} speeds up evaluation but can push a fit that fit comfortably
at one thread over \code{max_workload} at eight. Set \code{parallel_tape = FALSE}
to decouple the two: tapes then build one at a time (peak memory
unaffected by \code{n_cores}) while evaluation stays fully parallel.
\item \code{max_workload} (\code{NULL} auto-detects available memory; see above) is
checked against that workload number. Exceeding it does not always
error: under \code{method = "sml"} the fit auto-chunks instead (next point).
\code{Inf} disables the check \emph{and} auto-chunking entirely, so nothing sizes
the tape for you -- pin \code{tape_chunks} yourself if you still want the
memory benefit with the guard off.
\item \code{tape_chunks} (SML fits only; ignored under \code{method = "laplace"}, whose
tape scales with \code{nrow(data)} alone, not \code{draws}) is what absorbs a
workload over budget: \code{NULL} auto-selects the smallest chunk count that
brings the per-chunk workload back under \code{max_workload}; an explicit
value pins the layout (validated against both \code{draws} and the budget).
More chunks trade slower gradient evaluations (each chunk's contribution
is recomputed on every outer step) and the loss of a taped Hessian
(\code{confint(method = "profile")}/\code{rpbnb_tmb_dependence_profile()} fall
back to a Wald interval) for lower peak memory.
}

Net effect for a large SML fit: pick \code{n_cores} for speed first. If that
trips \code{max_workload}, either let auto-chunking absorb it, set
\code{parallel_tape = FALSE} if the extra peak memory isn't worth the
tape-build speedup, or raise \code{max_workload} deliberately against memory
you actually have (see \code{\link[=rpbnb_tmb_max_workload]{rpbnb_tmb_max_workload()}}).
}

\examples{
rpbnb_control(method = "BFGS", iterlim = 200)
rpbnb_control(hessian = "analytic")
# The same object drives either engine; the TMB-only knobs are simply
# ignored by the classic one (and reported as ignored in its summary).
rpbnb_control(n_cores = 4, gradtol = 1e-6, se_method = "opg")
}
\seealso{
\code{\link[=rpbnb_tmb_max_workload]{rpbnb_tmb_max_workload()}}, \code{\link[=rpbnb]{rpbnb()}}, \code{\link[=fit_rpbnb]{fit_rpbnb()}},
\code{\link[=fit_rpbnb_tmb]{fit_rpbnb_tmb()}}, \code{\link[=fit_bnb]{fit_bnb()}}
}
