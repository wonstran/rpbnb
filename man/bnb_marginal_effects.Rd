% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{bnb_marginal_effects}
\alias{bnb_marginal_effects}
\title{Marginal effects for a bivariate NB model}
\usage{
bnb_marginal_effects(
  fit,
  which = c("y1", "y2", "both", "all"),
  type = c("AME", "MEM"),
  vars = NULL,
  include_intercept = FALSE,
  digits = 4,
  print_output = TRUE
)
}
\arguments{
\item{fit}{A \code{bnb_fit} object from \code{\link[=fit_bnb]{fit_bnb()}}.}

\item{which}{Which margin(s): "y1", "y2", "both", or "all".}

\item{type}{"AME" (average marginal effect) or "MEM" (effect at the mean).}

\item{vars}{Optional variable names or indices to restrict output.}

\item{include_intercept}{Logical; include the intercept term.}

\item{digits}{Number of decimal places for printed output.}

\item{print_output}{Logical; if \code{FALSE}, suppress printing.}
}
\value{
A data frame (single margin) or a named list of data frames (both),
each with columns \code{Name}, \code{Estimate}, \code{StdErr}, \code{z}, \code{p}, \code{Signif},
\code{var_type}.
}
\description{
Average marginal effects (AME) or marginal effects at the mean (MEM) for
each margin. Continuous and binary (0/1) regressors are auto-detected;
continuous effects use \eqn{\partial E[Y]/\partial x_j = \beta_j \mu} and
binary effects use \eqn{E[Y|x_j=1] - E[Y|x_j=0]}.
}
\examples{
d <- read.csv(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb"))
fit <- fit_bnb(docvis ~ outwork + age, hospvis ~ outwork, data = d,
               dependence = "famoye")
bnb_marginal_effects(fit, which = "y1", type = "AME")
}
