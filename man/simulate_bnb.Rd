% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_bnb.R
\name{simulate_bnb}
\alias{simulate_bnb}
\title{Simulate data from the Famoye/Sarmanov bivariate NB distribution}
\usage{
simulate_bnb(
  n,
  beta1,
  beta2,
  dispersion = c(m1 = 0.5, m2 = 0.5),
  lambda = 0,
  covariates = NULL,
  seed = 1234
)
}
\arguments{
\item{n}{Number of observations.}

\item{beta1, beta2}{Named numeric vectors of coefficients for each equation;
must include \code{"(Intercept)"}.}

\item{dispersion}{Named numeric \code{c(m1 = ..., m2 = ...)} NB2 dispersion
parameters (variance = mu + m * mu^2).}

\item{lambda}{Famoye/Sarmanov dependence parameter. Must lie within the
valid bounds implied by the marginal means and dispersions.}

\item{covariates}{Optional data frame of covariates. If \code{NULL}, standard-
normal columns are generated for every non-intercept coefficient name.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A list with:
\describe{
\item{\code{data}}{data frame with \code{y1}, \code{y2}, and covariate columns}
\item{\code{mu}}{data frame with \code{mu1}, \code{mu2} (per-obs conditional means)}
\item{\code{true}}{list of true parameters: \code{beta1}, \code{beta2}, \code{dispersion}, \code{lambda}}
\item{\code{settings}}{list with \code{n} and \code{seed}}
\item{\code{meta}}{list with \code{seed} and \code{r_version}}
}
}
\description{
Generates paired count outcomes \verb{(y1, y2)} from the fixed-parameter
Famoye/Sarmanov bivariate NB2 joint PMF:
}
\details{
\deqn{P(Y_1=y_1, Y_2=y_2) = p_1(y_1)\,p_2(y_2)\,
  [1 + \lambda(e^{-y_1}-c_1)(e^{-y_2}-c_2)]}

where \eqn{c_k = E[e^{-Y_k}]} under NB2(\eqn{\mu_k, m_k}).
}
\examples{
sim <- simulate_bnb(n = 500,
  beta1 = c("(Intercept)" = 0.5, x1 = 0.3),
  beta2 = c("(Intercept)" = 0.2, x1 = -0.2),
  dispersion = c(m1 = 0.4, m2 = 0.5), lambda = 0.1, seed = 1)
head(sim$data)
}
