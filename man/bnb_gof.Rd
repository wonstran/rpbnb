% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{bnb_gof}
\alias{bnb_gof}
\title{Goodness of fit for a bivariate NB model}
\usage{
bnb_gof(fit, digits = 4, print_output = TRUE)
}
\arguments{
\item{fit}{A \code{bnb_fit} object from \code{\link[=fit_bnb]{fit_bnb()}}.}

\item{digits}{Number of decimal places for printed output.}

\item{print_output}{Logical; if \code{FALSE}, suppress printing and return the
result invisibly.}
}
\value{
Invisibly, a list with \code{n}, \code{k}, \code{logLik_full}, \code{logLik_null},
\code{AIC}, \code{BIC}, a named numeric vector \code{pseudoR2}, and the \code{null_fit}.
}
\description{
Reports log-likelihood, AIC, BIC, and four pseudo-R-squared measures
(McFadden, McFadden adjusted, Cox-Snell, Nagelkerke) relative to an
intercept-only null model refit with the same dependence structure (copula
fits rebuild the copula from \code{fit$cop_family}). A margin fitted with an
\code{offset()} keeps that offset in the null (an intercept-plus-offset model), so
the null shares the full model's exposure/sampling structure and the
pseudo-R-squared values remain comparable. Pseudo-R-squared values are
returned raw and are not clamped to \verb{[0, 1]}; a negative value flags a full
model that fits worse than the null.
}
\examples{
d <- read.csv(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb"))
fit <- fit_bnb(docvis ~ outwork + age, hospvis ~ outwork, data = d,
               dependence = "famoye")
bnb_gof(fit)
}
