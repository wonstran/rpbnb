% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpbnb.R
\name{rpbnb}
\alias{rpbnb}
\title{Fit a random-parameter bivariate NB model with either engine}
\usage{
rpbnb(
  formula_1,
  formula_2,
  data,
  engine = c("cpp", "tmb"),
  random_1 = NULL,
  random_2 = NULL,
  draws = 400,
  seed = 1234,
  start = NULL,
  dependence = "famoye",
  poisson_1 = FALSE,
  poisson_2 = FALSE,
  control = NULL,
  ...
)
}
\arguments{
\item{formula_1, formula_2}{Model formulas for the two count responses.}

\item{data}{A data frame containing the model variables.}

\item{engine}{Estimation engine: \code{"cpp"} (default) or \code{"tmb"}.}

\item{random_1, random_2}{Random-coefficient specifications for each equation.}

\item{draws}{Number of simulation draws.}

\item{seed}{Random seed for the draw sequence.}

\item{start}{Optional named or unnamed starting values.}

\item{dependence}{\code{"famoye"} (default), a \code{\link[=copula]{copula()}} object, or
\code{"independence"} (TMB engine only).}

\item{poisson_1, poisson_2}{Restrict the corresponding margin to its Poisson
limit.}

\item{control}{An \code{rpbnb_control()} object when \code{engine = "cpp"}, or an
\code{rpbnb_tmb_control()} object when \code{engine = "tmb"}. Defaults to the right
one for the chosen engine. The two are not interchangeable and are never
translated into one another.}

\item{...}{Further arguments passed to the selected fitter. Names are
validated against that fitter's formals; an argument belonging to the other
engine, or an unrecognised name, is an error.}
}
\value{
The engine-native fit object, identical to what a direct call to the
underlying fitter would return: an object of class \code{rpbnb_fit} for
\code{engine = "cpp"}, or \code{rpbnb_tmb_fit} for \code{engine = "tmb"}. The class
therefore depends on \code{engine}; test with \code{inherits(fit, "rpbnb_tmb_fit")}
if you need to branch. No wrapper class is introduced, so every existing S3
method and post-estimation function works unchanged.
}
\description{
A common front end over the package's two estimation engines. \code{engine = "cpp"} calls \code{\link[=fit_rpbnb]{fit_rpbnb()}} (Rcpp/OpenMP simulated likelihood, \code{maxLik} BFGS);
\code{engine = "tmb"} calls \code{\link[=fit_rpbnb_tmb]{fit_rpbnb_tmb()}} (TMB automatic differentiation,
\code{nlminb} with restart polish). Both fitters remain exported and can be called
directly; this wrapper adds nothing to the fit itself and returns exactly
what the chosen fitter returns.
}
\details{
What it does add is argument checking across the two APIs. The engines do not
take the same arguments, and passing one engine's argument to the other is a
mistake that is easy to make and expensive to notice — a standardized
coefficient table printed under an "original units" heading looks perfectly
plausible. Every extra argument is therefore matched by name against the
selected fitter's own formals, and anything that does not belong is an error
rather than a silently ignored \code{...} entry.
}
\section{Which arguments go with which engine}{
\tabular{lll}{
   Argument \tab \code{engine = "cpp"} \tab \code{engine = "tmb"} \cr
   \code{draw_type}, \code{.fixed}, \code{.opt_draws} \tab yes \tab error \cr
   \code{inference}, \code{keep}, \code{method} \tab error \tab yes \cr
   \code{offset()} in a formula \tab yes \tab error \cr
   \code{dependence = "independence"} \tab error \tab yes \cr
   \code{control} class \tab \code{rpbnb_control} \tab \code{rpbnb_tmb_control} \cr
   optimizer \tab \code{maxLik::maxLik(method = "BFGS")} \tab \code{stats::nlminb} + restarts \cr
}
}

\examples{
\donttest{
d <- read.csv(system.file("extdata", "rwm1984_bnb.csv", package = "rpbnb"))
fit <- rpbnb(docvis ~ outwork, hospvis ~ outwork, data = d,
             engine = "tmb", random_1 = "outwork", draws = 50)
}
}
\seealso{
\code{\link[=fit_rpbnb]{fit_rpbnb()}}, \code{\link[=fit_rpbnb_tmb]{fit_rpbnb_tmb()}}, \code{\link[=rpbnb_control]{rpbnb_control()}},
\code{\link[=rpbnb_tmb_control]{rpbnb_tmb_control()}}, \code{\link[=copula]{copula()}}, \code{\link[=fit_bnb]{fit_bnb()}}
}
