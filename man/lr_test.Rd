% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr_test.R
\name{lr_test}
\alias{lr_test}
\title{Likelihood-ratio test between two nested model fits}
\usage{
lr_test(restricted, full, boundary = FALSE)
}
\arguments{
\item{restricted}{The smaller (restricted) fit -- fewer estimated parameters.}

\item{full}{The larger (full) fit that nests \code{restricted}.}

\item{boundary}{Logical. When the restriction pins a variance/dispersion-type
parameter (a random-coefficient SD, or NB2 dispersion \code{m}) to its zero
boundary, the null distribution is not a plain chi-square. Set \code{boundary = TRUE} to use the 50:50 mixture of \code{chisq(df)} and \code{chisq(df - 1)} (Self &
Liang, 1987); for a single boundary parameter (\code{df = 1}) this halves the
naive p-value. Default \code{FALSE} (ordinary interior restriction, e.g. dropping
a fixed covariate). The mixture is exact only for a single parameter on the
boundary; simultaneous boundary restrictions of several parameters need
different weights and are not handled.}
}
\value{
An object of class \code{rpbnb_lrtest} with the LR \code{statistic}, degrees of
freedom \code{df}, \code{p.value}, the two log-likelihoods and their df, and the
\code{boundary} flag. Has a \code{print} method.
}
\description{
Compares a restricted fit against a full (nesting) fit by the likelihood-ratio
statistic. Works for any \code{\link[=fit_bnb]{fit_bnb()}} / \code{\link[=fit_rpbnb]{fit_rpbnb()}} objects, since both carry
a \code{logLik()} with a \code{"df"} attribute equal to the number of estimated
parameters.
}
\details{
The restricted model is one you fit yourself with a term removed -- for
example dropping a name from \code{random_1} (testing a random-coefficient SD), or
a plain NB / independence fit (testing an NB2 dispersion). This is the
statistically appropriate replacement for the Wald z/p that the natural-scale
summary suppresses on positive scale/dispersion parameters.

A likelihood-ratio test requires two \emph{maximized} likelihoods. When either
argument is a package fit (\code{bnb_fit} / \code{rpbnb_fit}) that records a failed
optimization (\code{convergence$converged = FALSE}), \code{lr_test()} errors rather than
returning a p-value from an unfinished fit -- the sign of the statistic cannot
establish convergence. Generic objects that only carry a \code{logLik()} (no
convergence record) are still accepted, but their convergence cannot be
validated and is the caller's responsibility.
}
\section{Famoye caveat for bounded random-coefficient distributions}{

Each Famoye fit maximizes its likelihood over an admissible lambda
interval frozen at that fit's own starting values. With normal or
lognormal random coefficients loaded in both margins the interval is the
constant \code{c(-1, 1)} for every fit, so the comparison is unaffected. With
uniform or triangular coefficients (or a single varying margin) the bound
moves with the parameters, so two fits frozen at different starting
values maximize over slightly different lambda ranges and the LR
statistic inherits that second-order discrepancy. Check
\code{lambda_admissible} on both fits before relying on a comparison in that
regime.
}

\examples{
sim <- simulate_rpbnb(n = 600,
  beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
  beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
  random_1 = list(x1 = list(sd = 0.5)),
  dispersion = c(m1 = 0.4, m2 = 0.5), seed = 1)
ctrl <- rpbnb_control(compute_se = FALSE)
full <- fit_rpbnb(y1 ~ x1, y2 ~ x1, data = sim$data, random_1 = "x1",
                  draws = 100, control = ctrl)
rest <- fit_rpbnb(y1 ~ x1, y2 ~ x1, data = sim$data,
                  draws = 100, control = ctrl)   # no random coefficient
lr_test(rest, full, boundary = TRUE)             # test sd(x1) = 0
}
\references{
Self, S. G. and Liang, K.-Y. (1987). Asymptotic properties of
maximum likelihood estimators and likelihood ratio tests under nonstandard
conditions. \emph{JASA} 82(398), 605--610.
}
