% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundary_tests.R
\name{bnb_boundary_tests}
\alias{bnb_boundary_tests}
\title{Boundary-corrected LR tests for the NB2 dispersions of a fit_bnb() fit}
\usage{
bnb_boundary_tests(fit, data, control = rpbnb_control(compute_se = FALSE))
}
\arguments{
\item{fit}{A converged \code{bnb_fit} (from \code{\link[=fit_bnb]{fit_bnb()}}) with \code{dependence = "independence"} or \code{"famoye"}. Not supported for a \code{\link[=copula]{copula()}} dependence:
\code{fit_bnb()} itself refuses to combine \code{poisson_1}/\code{poisson_2} with a
copula dependence, so there is no Poisson-limit restriction to test.}

\item{data}{The data frame the model was fit on. Required -- the fit object
does not store it, and every restricted model is refit on it.}

\item{control}{An \code{\link[=rpbnb_control]{rpbnb_control()}} for the restricted refits. Defaults to
\code{compute_se = FALSE} (the LR test needs only \code{logLik} and the degrees of
freedom).}
}
\value{
An object of class \code{bnb_boundary_tests} (a data frame with columns
\code{Parameter}, \code{LR}, \code{df}, \code{p.value}, \code{Signif}, one row per tested margin)
and a \code{print} method. A margin already fit at \code{poisson_1}/\code{poisson_2 = TRUE} has no free dispersion left to test and is skipped, matching
\code{\link[=rpbnb_boundary_tests]{rpbnb_boundary_tests()}}'s dispersion-test behaviour.
}
\description{
Runs a likelihood-ratio test for each free NB2 dispersion (\code{m1}, \code{m2}) of a
fitted fixed-coefficient bivariate NB model, against a restricted refit at
that margin's exact Poisson limit (\code{m = 0}). \code{m = 0} sits on the boundary of
the parameter space, so the natural-scale summary reports no Wald \code{z}/\code{p}
for \code{m1}/\code{m2} (see \code{\link[=fit_bnb]{fit_bnb()}}); the valid test is \code{\link[=lr_test]{lr_test()}}'s
\code{boundary = TRUE} 50:50 chi-square mixture. This is the fixed-coefficient
counterpart of \code{\link[=rpbnb_boundary_tests]{rpbnb_boundary_tests()}} -- the same test, restricted to the
one boundary-null group \code{fit_bnb()} has (no random-coefficient scales).
}
\details{
Each restricted refit is warm-started from the full fit's own coefficients
(\code{start = fit$coef}); \code{fit_bnb()} pins the tested margin's \code{log_m} to the
Poisson placeholder regardless of what \code{start} supplies for it, so passing
the full vector is safe and needs no per-test trimming. Unlike
\code{rpbnb_boundary_tests()}, there is no warm-start "polish" step for a
negative LR statistic: \code{fit_bnb()}'s likelihood is exact (\code{glm.nb} for
independence, closed-form-gradient BFGS for Famoye), not simulated, so a
warm-started restricted refit does not out-climb the full fit's own optimum
the way a simulated objective occasionally can.
}
\examples{
d <- read.csv(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb"))
fit <- fit_bnb(docvis ~ outwork, hospvis ~ outwork, data = d,
               dependence = "famoye")
bnb_boundary_tests(fit, d)
}
\seealso{
\code{\link[=lr_test]{lr_test()}}, \code{\link[=fit_bnb]{fit_bnb()}}, \code{\link[=rpbnb_boundary_tests]{rpbnb_boundary_tests()}}
}
