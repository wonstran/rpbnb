# Review of `response_2026-08-08-15-26-11.md`

**Reviewed:** 2026-08-08 21:49:51 America/New_York

**Target:** `merge-tmb-engine` at `75a9ca3`, compared with parent `044cc9c`

## Verdict

The implementation now maps and reports lambda through the interval used by the optimized objective, checks that value against the interval at the optimum, retains both intervals and the admissibility flag, and uses the objective's frozen interval in all three covariance paths. I found no remaining implementation defect in the two fixes. The regression tests, however, do not yet exercise cases that would fail if the fixed-interval wiring were removed again.

## Finding

### P2 - The new regressions do not distinguish the corrected integration paths from the previous broken ones

**Locations:** `tests/testthat/test-fit-rpbnb.R:196-256`, `tests/testthat/test-cpp-likelihood.R:90-105`, `tests/testthat/test-rpbnb-hessian.R:1-69`; compare `R/fit_rpbnb.R:448-512` and `R/halton_parallel.R:176-212`.

The arithmetic test at `test-fit-rpbnb.R:196-211` demonstrates why the old check was tautological, but it reimplements the mapping locally and never calls the production guard. The end-to-end test comments that the frozen and optimum intervals "genuinely differ," but does not assert that they differ, does not produce an inadmissible fit, and uses `compute_se = FALSE`. It therefore never enters any of the OPG, analytic-Hessian, or numeric-Hessian branches fixed by this commit.

The existing score identity test calls both wrappers with `lam_bounds = NULL`, so it validates only the recomputed-bound default. The Hessian tests validate the lower-level analytic formula against a numeric objective with an explicitly supplied interval, but do not verify that `fit_rpbnb()` passes its start-value interval rather than the interval at the optimum. These tests all passed at `044cc9c`, when the fit-level covariance wiring was still wrong, and the three new fast assertions are only the standalone arithmetic checks. The response's claim that there is "no longer a path by which they can diverge" overlooks the exact path that caused the defect: a caller can omit the new argument, causing the score wrapper to silently recompute its own interval.

Add a parameter-dependent fixture with distinct frozen and evaluation-point bounds. Pass the frozen interval to both `bnbr_rp_ll_and_grad_cpp()` and `bnbr_rp_scores_cpp()` and assert `colSums(scores)` equals that frozen-objective gradient; this fails if either the caller or score wrapper drops `lam_bounds`. For the fit-level guard, at minimum assert `fit$bounds` and `fit$bounds_at_optimum` differ in the chosen fixture; preferably construct an escaped fit and assert the production warning and `lambda_admissible = FALSE`. A fit-level covariance check for each selected `se_method` would also pin the interval threaded into the Hessian branches.

## Confirmed corrections

- `lam_hat_used` is mapped through `lam_frozen`, checked against the interval at the optimum, returned as `fit$lambda`, and retained alongside `bounds`, `bounds_at_optimum`, and `lambda_admissible`.
- OPG scores receive `lam_bounds = lam_frozen`; analytic and numeric Hessians receive the same interval explicitly.
- `summary.rpbnb_fit()` now obtains the correct frozen-interval width through `fit$bounds` for the lambda delta method.
- Copula fits remain compatible with the expanded constructor through its default values.

## Verification performed

- Inspected the complete `044cc9c..75a9ca3` implementation and test diff and all callers of `new_rpbnb_fit()`, `bnbr_rp_scores_cpp()`, and `bnbr_rp_hessian()`.
- Parsed all 91 R source and test files successfully with R 4.6.1.
- `git diff --check 044cc9c..75a9ca3` reported no whitespace errors.

The current R library does not provide `rpbnb`, `testthat`, `TMB`, or `Rcpp`, so the response's compiled-suite results were not independently rerun.
