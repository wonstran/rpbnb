# Fifth follow-up review: TMB SML OOM plan response

Reviewed `comments/response_2026-08-24-16-33-35.md` against `comments/review_2026-08-24-16-31-12.md` and the revised plan.

## Findings

No further findings.

The response resolves both remaining plan-level issues:

- The plan now commits to one shared conservative calibration across the static and chunked graphs. Taking the component-wise maxima makes the product conservative even when the graph supplying the larger slope differs from the graph supplying a larger family or mode weight. The resolver, default-budget conversion, predicted-peak reporting, and both tape branches therefore use one workload-unit meaning. The branch-alternation regression directly covers the failure mode raised in the prior review.
- The resolver now short-circuits `max_workload = Inf` before finite-budget arithmetic. Its three specified cases preserve the existing opt-out contract: automatic layout remains the one-chunk static path without a message, a valid pinned layout is honored, and the one-draw hard stop is bypassed.

## Disposition

The plan is ready for implementation. The remaining concerns are implementation-verification items already captured by the plan: measure both graphs before publishing the shared constants, verify conditional `DATA_UPDATE` behavior in the shipped TMB version, and run the listed equivalence, workload, memory, retained-object, and boundary-refit tests. No code change is asserted by this response.
