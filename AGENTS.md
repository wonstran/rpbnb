# rpbnb — Agent Instructions

## This is an R package

- Language: R (≥ 4.1)
- Package name: `rpbnb`
- Build: roxygen2, testthat (edition 3), pkgdown, devtools
- RStudio project: `rpbnb.Rproj` (`BuildType: Package`, roxygen on build)
- No JavaScript or Python. Compiled code IS present: a multithreaded Rcpp/OpenMP
  core lives in `src/` (`halton_parallel.cpp`, `copula_parallel.cpp`,
  `RcppExports.cpp`); DESCRIPTION declares `LinkingTo: Rcpp` and `Imports: Rcpp`.

## Commands

```r
# In R console from the package root:
devtools::load_all()     # load package for interactive work
devtools::document()     # regenerate NAMESPACE + man/
devtools::test()         # run all tests
devtools::test_active_file()  # run tests in the current file
devtools::check()        # full R CMD check
devtools::install()      # install the package
pkgdown::build_site()    # rebuild docs/ website
```

**To run a single test file** from the R console:
```r
testthat::test_file("tests/testthat/test-fit-bnb.R")
```

**To run tests from terminal** (no testthat reporter on errors, less useful):
```r
Rscript -e 'devtools::test()'
```

## Never edit these — they're generated

- `NAMESPACE` — generated by roxygen2 (`devtools::document()`)
- `man/*.Rd` — generated by roxygen2
- `docs/` — generated by pkgdown (the reference site)

## Architecture

Three user-facing model paths, all producing S3 objects:

| Function | Class | Estimator |
|----------|-------|-----------|
| `fit_bnb(dependence = "independence")` | `bnb_fit` | Two `MASS::glm.nb` margins |
| `fit_bnb(dependence = "famoye")` | `bnb_fit` | maxLik BFGS, analytic gradient |
| `fit_bnb(dependence = copula(...))` | `bnb_fit` | maxLik BFGS, copula gradient |
| `fit_rpbnb()` | `rpbnb_fit` | maxLik BFGS, MSL with Halton draws |

Supporting: `simulate_bnb()`, `simulate_rpbnb()`, `rpbnb_control()`, `copula()`, plus S3 methods for `print`, `summary`, `coef`, `vcov`, `logLik`, `predict`, `AIC`, `BIC`.

**Source layout** in `R/` (plus a compiled Rcpp/OpenMP core in `src/`):
- `fit_bnb.R` — public `fit_bnb()` + three internal fitters (famoye, copula, independence)
- `fit_rpbnb.R` — public `fit_rpbnb()` + internal MSL estimator
- `famoye_core.R` — Famoye/Sarmanov bivariate NB log-likelihood + analytic gradient + Hessian
- `copula_core.R`, `copula_likelihood.R`, `copula.R` — copula infrastructure
- `rpbnb_likelihood.R`, `simulation_draws.R` — RP-BNB likelihood + Halton draw generation
- `rand_dist.R` — random-coefficient distribution registry (normal, lognormal, uniform, triangular)
- `data_prep.R` — formula-to-matrix preprocessing (`.prepare_bnb_data`)
- `simulate_bnb.R`, `simulate_rpbnb.R` — data simulators
- `diagnostics.R` — `bnb_gof()`, `bnb_marginal_effects()`, `bnb_elasticities()`
- `methods.R` — S3 methods (print, summary, coef, vcov, logLik, predict)
- `control.R` — `rpbnb_control()`
- `utilities.R` — helpers
- `rpbnb-package.R` — package doc + imports

**Internal visibility**: Internal functions use `@keywords internal @noRd`. Public objects in `NAMESPACE` are defined via `@export` roxygen tags. Do not add exports manually to NAMESPACE.

## Key conventions

- **2-space indent** (configured in `rpbnb.Rproj`)
- **S3 dispatch** — the class determines method behavior; `class(fit)` returns `c("bnb_fit")` or `c("rpbnb_fit")`
- **Constructor pattern** — `new_bnb_fit()` / `new_rpbnb_fit()` build the object; public `fit_*()` functions are thin wrappers
- **`rpbnb_control()`** — all tuning params (iterlim, reltol, hessian, n_cores, etc.) funnel through one control object
- **Seed semantics** — `set.seed(seed)` drives the Cranley-Patterson rotation in `halton_uniform()`; it affects reproducibility of Halton draws, not RNG
- **Lambda bounds** — the Famoye dependence parameter λ has data-adaptive bounds; the Hessian freezes bounds at the optimum for valid SEs

## Testing

- Test files live in `tests/testthat/`, no custom helpers
- Tests use `rpbnb:::` to access internal functions — this is standard for package tests
- Key test data: `system.file("extdata", "rwm1984_clean.csv", package = "rpbnb")` — skip tests if this file is unavailable (some tests do this)
- Some tests fit real models and can be slow; no flaky tests known
- `testthat` edition 3 (`Config/testthat/edition: 3` in DESCRIPTION)

## NP-hard gotchas

1. **`parallel::clusterExport` in `fit_rpbnb()`** (lines ~151-155 and ~219-223): If you add a new helper function called inside the parallelized likelihood/gradient path, you MUST add its name to the `clusterExport` list. Workers run in a bare `globalenv` without the package namespace.

2. **Frozen lambda bounds**: The analytic gradient for Famoye BNB treats λ bounds as frozen at the current parameters. The optimizer's actual objective uses moving (data-adaptive) bounds. Both are correct — the Hessian must use the frozen version — just don't be surprised that `bnbr_loglik_fixed_bounds` != `sum(bnb_loglik_vec)` in general (they match at the optimum where bounds stabilize).

3. **`rand_dist_registry`** in `rand_dist.R`: adding a new distribution requires a registry entry with `transform`, `grad_xform`, `scale_label`, gradient, and Hessian closures. Both `fit_rpbnb()` and `simulate_rpbnb()` consume this registry — change it in one place and both paths pick it up.

4. **Standard errors for `rpbnb_fit`**: The numeric/analytic/OPG Hessian is taken with the SAME optimization draws that produced the estimate (same-draw curvature), so the SEs are the curvature of the objective that was optimized. The `draws_hessian` control param is retained for backward compatibility but is a no-op — do NOT reintroduce a separate `seed + 1` Hessian draw set (that was removed intentionally). Repair of a non-positive-definite information matrix is recorded on `fit$hessian_diag` and warned about, not silent.

## References

- `dev-docs/superpowers/specs/` — design specs for implemented features (the
  TMB-engine specs moved here from `docs/` in the 0.4.0 merge; `docs/` is
  pkgdown output that `build_site()` cleans)
- `docs/llms.txt` — machine-readable package index (subset of README)
- `inst/legacy/` — original reference scripts (preserved for comparison, not used by the package)
- `NEWS.md` — changelog
- `vignettes/rpbnb-intro.Rmd` — introductory vignette
