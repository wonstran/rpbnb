# rpbnb Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Package the two mature scripts (`Rcodes/bnbr_v2-4.R` Famoye BNB MLE and `Rcodes/rpbnbr_faymore.R` random-parameter BNB SIML) into an installable, tested R package `rpbnb`, deduplicating their shared core math, with a minimal `simulate_rpbnb()`, an `independence` dependence option, S3 methods, roxygen2 docs, a vignette, and a `testthat` suite that passes `R CMD check`.

**Architecture:** A single shared internal file `R/famoye_core.R` holds the NB2 + Famoye math (currently duplicated across both scripts). `fit_bnb()` and `fit_rpbnb()` build design matrices, call `maxLik`/BFGS with analytic gradients, compute SEs from a frozen-bounds numeric Hessian, and return S3 objects (`bnb_fit`, `rpbnb_fit`). Diagnostics, S3 methods, the simulator, and Halton draws are separate focused files. Math is unchanged from the source; correctness is pinned by reproduction tests against the legacy scripts and against base-R `dnbinom`.

**Tech Stack:** R (≥ 4.1), `maxLik`, `numDeriv`, `randtoolbox`, `MASS`, `stats`; dev tooling `testthat` (3e), `roxygen2`, `devtools`, `knitr`/`rmarkdown`.

**Reference spec:** `docs/superpowers/specs/2026-06-13-rpbnb-phase1-design.md`

**Port-source note:** When a step says "port from `Rcodes/<file>.R` lines A–B", open that file, copy those functions verbatim into the target file, then apply only the listed edits (rename, add roxygen, prefix namespaces). The math must stay byte-identical; reproduction tests will catch drift.

**Environment note (Windows):** Run R from the project root. If `Rscript`/`R` are not on PATH, locate via `where Rscript` (cmd) or check `C:\Program Files\R\R-*\bin\`. All test runs below use `Rscript -e "..."` or `devtools::test()`. The package root is the project root `D:\apps\mvnb` (the package lives at the top level so `inst/`, `R/`, `DESCRIPTION` sit beside the existing `Rcodes/`, `data/`, `docs/`).

---

## Task 0: Package skeleton

**Files:**
- Create: `DESCRIPTION`
- Create: `NAMESPACE`
- Create: `LICENSE`
- Create: `R/rpbnb-package.R`
- Create: `tests/testthat.R`
- Create: `.Rbuildignore`

- [ ] **Step 1: Create `DESCRIPTION`**

```
Package: rpbnb
Type: Package
Title: Bivariate and Random-Parameter Negative Binomial Regression
Version: 0.1.0
Authors@R: person("Project", "Author", email = "wonstran@gmail.com", role = c("aut", "cre"))
Description: Maximum-likelihood estimation of the Famoye/Sarmanov bivariate
    negative binomial (NB2) regression model and maximum-simulated-likelihood
    estimation of a bivariate random-parameter negative binomial model, with a
    normal random-coefficient data simulator, diagnostics, and standard model
    methods.
License: MIT + file LICENSE
Encoding: UTF-8
Depends: R (>= 4.1)
Imports:
    stats,
    maxLik,
    numDeriv,
    randtoolbox,
    MASS
Suggests:
    testthat (>= 3.0.0),
    knitr,
    rmarkdown,
    parallel
Config/testthat/edition: 3
VignetteBuilder: knitr
Roxygen: list(markdown = TRUE)
RoxygenNote: 7.3.2
LazyData: false
```

- [ ] **Step 2: Create `LICENSE`**

```
YEAR: 2026
COPYRIGHT HOLDER: rpbnb authors
```

- [ ] **Step 3: Create `R/rpbnb-package.R`** (package-level roxygen + import declarations)

```r
#' rpbnb: Bivariate and Random-Parameter Negative Binomial Regression
#'
#' Maximum-likelihood estimation of the Famoye/Sarmanov bivariate negative
#' binomial model and maximum-simulated-likelihood estimation of a bivariate
#' random-parameter negative binomial model.
#'
#' @keywords internal
"_PACKAGE"

#' @importFrom stats model.frame model.matrix model.response pnorm plogis
#'   qnorm symnum coef logLik AIC BIC predict vcov dnbinom rnorm runif rbinom
#'   var sd setNames
#' @importFrom utils head
NULL
```

- [ ] **Step 4: Create `tests/testthat.R`**

```r
library(testthat)
library(rpbnb)

test_check("rpbnb")
```

- [ ] **Step 5: Create `.Rbuildignore`**

```
^Rcodes$
^data/rwm1984\.csv$
^docs$
^\.git$
^\.gitignore$
^\.claude$
^.*\.Rproj$
^\.Rproj\.user$
^LICENSE\.md$
```

- [ ] **Step 6: Create empty `NAMESPACE`** (roxygen2 will overwrite it)

```
# Generated by roxygen2: do not edit by hand
```

- [ ] **Step 7: Verify the skeleton loads**

Run: `Rscript -e "devtools::load_all('.'); cat('loaded\n')"`
Expected: prints `loaded` with no error (warnings about no exported objects are fine).

- [ ] **Step 8: Commit**

```bash
git add DESCRIPTION NAMESPACE LICENSE R/rpbnb-package.R tests/testthat.R .Rbuildignore
git commit -m "feat: rpbnb package skeleton"
```

---

## Task 1: Shared core math (`famoye_core.R`)

The single source of truth for the NB2 + Famoye math currently duplicated in both scripts.

**Files:**
- Create: `R/famoye_core.R`
- Test: `tests/testthat/test-famoye-core.R`

- [ ] **Step 1: Write the failing test**

`tests/testthat/test-famoye-core.R`:

```r
test_that("nb_logpmf_y_mu_r matches stats::dnbinom", {
  y  <- 0:10
  mu <- 2.3
  m  <- 0.7
  r  <- 1 / m
  expect_equal(
    rpbnb:::nb_logpmf_y_mu_r(y, mu, r),
    stats::dnbinom(y, size = r, mu = mu, log = TRUE),
    tolerance = 1e-10
  )
})

test_that("c_val equals E[exp(-Y)] on a truncated NB2 grid", {
  mu <- 1.5; m <- 0.8; r <- 1 / m
  y  <- 0:2000
  approx <- sum(exp(-y) * stats::dnbinom(y, size = r, mu = mu))
  expect_equal(rpbnb:::c_val(mu, m), approx, tolerance = 1e-8)
})

test_that("lambda_bounds_vec returns a valid open interval", {
  c1 <- c(0.2, 0.4, 0.6); c2 <- c(0.3, 0.5, 0.1)
  b  <- rpbnb:::lambda_bounds_vec(c1, c2)
  expect_length(b, 2)
  expect_true(b[1] < 0 && b[2] > 0 && b[1] < b[2])
})

test_that("d_const is Famoye's 1 - exp(-1)", {
  expect_equal(rpbnb:::d_const(), 1 - exp(-1), tolerance = 1e-12)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-famoye-core.R')"`
Expected: FAIL — `could not find function "nb_logpmf_y_mu_r"`.

- [ ] **Step 3: Write `R/famoye_core.R`**

Port the helpers from `Rcodes/bnbr_v2-4.R` lines 154–184 (`c_val`, `nb_logpmf_y_mu_r`, `lambda_bounds_vec`, `dct_dm`, `dc_dbeta_mat`). Change `d_const` from a top-level constant to a function `d_const()` to avoid an undocumented global (scope §11). All functions internal (no `@export`). Use this exact content:

```r
# Famoye/Sarmanov bivariate NB (NB2) core math. Internal. Single source of truth.

#' Famoye's d constant: 1 - exp(-1)
#' @keywords internal
#' @noRd
d_const <- function() 1 - exp(-1)

#' E[exp(-Y)] under NB2(mu, m); c(mu, m) = (1 + d*m*mu)^(-1/m)
#' @keywords internal
#' @noRd
c_val <- function(mu, m) (1 + d_const() * m * mu)^(-1 / m)

#' NB2 log pmf with mean mu and size r = 1/m
#' @keywords internal
#' @noRd
nb_logpmf_y_mu_r <- function(y, mu, r) {
  p <- r / (r + mu)
  lgamma(y + r) - lgamma(r) - lgamma(y + 1) + r * log(p) + y * log1p(-p)
}

#' Conservative global lambda-bounds from per-obs c1, c2
#' @keywords internal
#' @noRd
lambda_bounds_vec <- function(c1, c2) {
  lam_min <- -1 / ((1 - c1) * (1 - c2))
  lam_max <-  1 / pmax(c1 * (1 - c2), c2 * (1 - c1))
  c(max(lam_min), min(lam_max))
}

#' d c(mu,m) / d m
#' @keywords internal
#' @noRd
dct_dm <- function(mu, m, c) {
  m_inv <- 1 / m
  denom <- 1 + d_const() * m * mu
  term  <- m_inv * (m_inv * log(denom) - (d_const() * mu) / denom)
  term * c
}

#' d c(mu,m) / d beta_j as an n x p matrix
#' @keywords internal
#' @noRd
dc_dbeta_mat <- function(mu, m, c, X) {
  denom <- 1 + d_const() * m * mu
  row_factor <- -(d_const() * c * mu) / denom
  sweep(X, 1, row_factor, `*`)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-famoye-core.R')"`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add R/famoye_core.R tests/testthat/test-famoye-core.R
git commit -m "feat: shared Famoye/NB2 core math with dnbinom-pinned tests"
```

---

## Task 2: Utilities (`utilities.R`)

**Files:**
- Create: `R/utilities.R`
- Test: `tests/testthat/test-utilities.R`

- [ ] **Step 1: Write the failing test**

`tests/testthat/test-utilities.R`:

```r
test_that("row_log_sum_exp matches log(rowSums(exp(.)))", {
  set.seed(1)
  M <- matrix(rnorm(20, sd = 5), nrow = 4)
  expect_equal(
    rpbnb:::row_log_sum_exp(M),
    log(rowSums(exp(M))),
    tolerance = 1e-10
  )
})

test_that("row_log_sum_exp is stable for large magnitudes", {
  M <- matrix(c(1000, 1001, 1002, 1000), nrow = 2)
  out <- rpbnb:::row_log_sum_exp(M)
  expect_true(all(is.finite(out)))
})

test_that("accepted_ll_trace keeps only improving values", {
  tr <- rpbnb:::accepted_ll_trace(c(-10, -8, -9, -7, -7.0000001))
  expect_equal(tr$logLik, c(-10, -8, -7), tolerance = 1e-6)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-utilities.R')"`
Expected: FAIL — `could not find function "row_log_sum_exp"`.

- [ ] **Step 3: Write `R/utilities.R`**

`row_log_sum_exp` ports `rowLogSumExp` from `Rcodes/rpbnbr_faymore.R` lines 49–52. `accepted_ll_trace` ports `get_iteration_trace` from `Rcodes/bnbr_v2-4.R` lines 34–42.

```r
#' Numerically stable per-row log-sum-exp
#' @keywords internal
#' @noRd
row_log_sum_exp <- function(M) {
  m <- apply(M, 1, max)
  m + log(rowSums(exp(M - m)))
}

#' Trace of strictly-improving log-likelihood values
#' @keywords internal
#' @noRd
accepted_ll_trace <- function(ll_vals, tol = 1e-8) {
  ll_vals <- ll_vals[is.finite(ll_vals)]
  if (!length(ll_vals)) {
    return(data.frame(iter = integer(0), logLik = numeric(0)))
  }
  best <- -Inf; iter <- integer(0); val <- numeric(0)
  for (i in seq_along(ll_vals)) {
    if (ll_vals[i] > best + tol) {
      best <- ll_vals[i]
      iter <- c(iter, length(iter) + 1L)
      val  <- c(val, ll_vals[i])
    }
  }
  data.frame(iter = iter, logLik = val)
}

#' Significance stars for a vector of p-values
#' @keywords internal
#' @noRd
signif_stars <- function(p) {
  as.character(stats::symnum(
    p, corr = FALSE, na = FALSE,
    cutpoints = c(0, .001, .01, .05, .1, 1),
    symbols   = c("***", "**", "*", ".", " ")
  ))
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-utilities.R')"`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add R/utilities.R tests/testthat/test-utilities.R
git commit -m "feat: numeric/printing utilities"
```

---

## Task 3: Control object (`rpbnb_control`)

**Files:**
- Create: `R/control.R`
- Test: `tests/testthat/test-control.R`

- [ ] **Step 1: Write the failing test**

`tests/testthat/test-control.R`:

```r
test_that("rpbnb_control returns documented defaults", {
  ctl <- rpbnb_control()
  expect_s3_class(ctl, "rpbnb_control")
  expect_equal(ctl$method, "BFGS")
  expect_equal(ctl$iterlim, 300)
  expect_equal(ctl$draws_hessian, 100)
  expect_equal(ctl$n_cores, 1L)
  expect_true(ctl$compute_se)
})

test_that("rpbnb_control validates inputs", {
  expect_error(rpbnb_control(n_cores = 0), "n_cores")
  expect_error(rpbnb_control(method = "NOPE"), "method")
  expect_error(rpbnb_control(draws_hessian = -1), "draws_hessian")
})

test_that("rpbnb_control overrides take effect", {
  ctl <- rpbnb_control(method = "NR", iterlim = 50, compute_se = FALSE)
  expect_equal(ctl$method, "NR")
  expect_equal(ctl$iterlim, 50)
  expect_false(ctl$compute_se)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-control.R')"`
Expected: FAIL — `could not find function "rpbnb_control"`.

- [ ] **Step 3: Write `R/control.R`**

```r
#' Control parameters for rpbnb estimators
#'
#' @param method Optimizer passed to [maxLik::maxLik()]. One of "BFGS", "NR",
#'   "BHHH", "NM".
#' @param iterlim Maximum optimizer iterations.
#' @param reltol Relative convergence tolerance.
#' @param print_level Verbosity passed to the optimizer (0 = silent).
#' @param draws_hessian Number of simulation draws used for the random-parameter
#'   Hessian (smaller than the optimization draws for speed). Ignored by [fit_bnb()].
#' @param halton_burn,halton_skip Halton sequence burn-in and skip.
#' @param n_cores Worker processes for the optional cluster path (1 = sequential).
#' @param compute_se If FALSE, skip the Hessian and standard errors.
#' @param hess_eps,hess_r Step and Richardson order for [numDeriv::hessian()].
#'
#' @return An object of class `rpbnb_control` (a named list).
#' @export
#' @examples
#' rpbnb_control(method = "BFGS", iterlim = 200)
rpbnb_control <- function(method = c("BFGS", "NR", "BHHH", "NM"),
                          iterlim = 300L,
                          reltol = 1e-8,
                          print_level = 0L,
                          draws_hessian = 100L,
                          halton_burn = 300L,
                          halton_skip = 100L,
                          n_cores = 1L,
                          compute_se = TRUE,
                          hess_eps = 1e-5,
                          hess_r = 4L) {
  if (length(method) > 1) method <- method[1]
  allowed <- c("BFGS", "NR", "BHHH", "NM")
  if (!method %in% allowed) {
    stop("`method` must be one of: ", paste(allowed, collapse = ", "), call. = FALSE)
  }
  if (!is.numeric(n_cores) || n_cores < 1) {
    stop("`n_cores` must be a positive integer.", call. = FALSE)
  }
  if (!is.numeric(draws_hessian) || draws_hessian < 1) {
    stop("`draws_hessian` must be a positive integer.", call. = FALSE)
  }
  structure(
    list(method = method, iterlim = as.integer(iterlim), reltol = reltol,
         print_level = as.integer(print_level),
         draws_hessian = as.integer(draws_hessian),
         halton_burn = as.integer(halton_burn),
         halton_skip = as.integer(halton_skip),
         n_cores = as.integer(n_cores), compute_se = isTRUE(compute_se),
         hess_eps = hess_eps, hess_r = as.integer(hess_r)),
    class = "rpbnb_control"
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-control.R')"`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add R/control.R tests/testthat/test-control.R
git commit -m "feat: rpbnb_control with validation"
```

---

## Task 4: Halton draws (`simulation_draws.R`)

**Files:**
- Create: `R/simulation_draws.R`
- Test: `tests/testthat/test-simulation-draws.R`

- [ ] **Step 1: Write the failing test**

`tests/testthat/test-simulation-draws.R`:

```r
test_that("halton_normal returns the requested shape", {
  Z <- rpbnb:::halton_normal(n_draws = 200, d = 3, burn = 50, skip = 10)
  expect_equal(dim(Z), c(200L, 3L))
  expect_true(all(is.finite(Z)))
})

test_that("halton_normal columns are approximately standard normal", {
  Z <- rpbnb:::halton_normal(n_draws = 2000, d = 2, burn = 100, skip = 10)
  expect_lt(abs(mean(Z[, 1])), 0.1)
  expect_lt(abs(sd(Z[, 1]) - 1), 0.1)
})

test_that("d = 0 yields a 0-column matrix with n_draws rows", {
  Z <- rpbnb:::halton_normal(n_draws = 10, d = 0)
  expect_equal(dim(Z), c(10L, 0L))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-simulation-draws.R')"`
Expected: FAIL — `could not find function "halton_normal"`.

- [ ] **Step 3: Write `R/simulation_draws.R`**

Ports `make_halton_norm` from `Rcodes/rpbnbr_faymore.R` lines 53–57. Add the `d == 0` guard and use `usetime = FALSE` so draws are reproducible across runs (the source used `usetime = TRUE`; for a smooth, seed-stable simulated likelihood we want determinism — see spec §3 "same draws retained").

```r
#' Generate standard-normal draws from a Halton sequence
#'
#' @param n_draws Number of draws (rows).
#' @param d Dimension (columns). If 0, a 0-column matrix is returned.
#' @param skip,burn Halton skip and burn-in.
#' @return An `n_draws` x `d` numeric matrix of standard-normal values.
#' @keywords internal
#' @noRd
halton_normal <- function(n_draws, d, skip = 100, burn = 200) {
  if (d <= 0) return(matrix(0, nrow = n_draws, ncol = 0))
  n <- burn + n_draws
  U <- randtoolbox::halton(n = n, dim = d, normal = FALSE,
                           usetime = FALSE, init = TRUE)
  U <- matrix(U, nrow = n, ncol = d)
  qnorm(U[(burn + 1):n, , drop = FALSE])
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-simulation-draws.R')"`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add R/simulation_draws.R tests/testthat/test-simulation-draws.R
git commit -m "feat: deterministic Halton normal draws"
```

---

## Task 5: BNB likelihood internals + `fit_bnb()` (Famoye + independence)

This is the largest task. Split into the likelihood internals (5a), the independence path (5b), and the public `fit_bnb()` wrapper that returns a `bnb_fit` object (5c).

**Files:**
- Create: `R/bnb_likelihood.R` (internal LL/score/gradient/fixed-bounds)
- Create: `R/fit_bnb.R` (public `fit_bnb`, `bnb_fit` constructor, independence path)
- Test: `tests/testthat/test-fit-bnb.R`

### Task 5a: Likelihood internals

- [ ] **Step 1: Write `R/bnb_likelihood.R`**

Port verbatim from `Rcodes/bnbr_v2-4.R`: `bnb_loglik_vec` (lines 186–214), `bnb_score_mat` (216–272), `bnb_grad_vec` (274–275), `bnbr_loglik_fixed_bounds` (277–301). Remove their local re-definitions of core math (now in `famoye_core.R`). Mark all `@keywords internal` / `@noRd`. The parameter vector is `[beta1 (p1), beta2 (p2), log_m1, log_m2, z_lambda]`. Keep `eps <- 1e-6` and the `pmax(dep, 1e-300)` floor exactly. No signature changes.

- [ ] **Step 2: Write a direct unit test for the likelihood vs an independent reconstruction**

Add to `tests/testthat/test-fit-bnb.R`:

```r
test_that("bnb_loglik_vec equals two NB2 logs + dependence term at lambda interior", {
  set.seed(7)
  n  <- 50
  X1 <- cbind(1, rnorm(n)); X2 <- cbind(1, rnorm(n))
  y1 <- rpois(n, 2); y2 <- rpois(n, 2)
  beta1 <- c(0.3, 0.1); beta2 <- c(0.2, -0.1)
  log_m1 <- log(0.5); log_m2 <- log(0.6); zlam <- 0
  par <- c(beta1, beta2, log_m1, log_m2, zlam)

  got <- rpbnb:::bnb_loglik_vec(par, y1, y2, X1, X2)

  # independent reconstruction
  m1 <- exp(log_m1); m2 <- exp(log_m2); r1 <- 1/m1; r2 <- 1/m2
  mu1 <- as.vector(exp(X1 %*% beta1)); mu2 <- as.vector(exp(X2 %*% beta2))
  c1 <- (1 + (1-exp(-1))*m1*mu1)^(-1/m1)
  c2 <- (1 + (1-exp(-1))*m2*mu2)^(-1/m2)
  b  <- rpbnb:::lambda_bounds_vec(c1, c2)
  lam <- b[1] + (b[2]-b[1]) * (1e-6 + (1-2e-6)*plogis(zlam))
  exp_ll <- dnbinom(y1, size = r1, mu = mu1, log = TRUE) +
            dnbinom(y2, size = r2, mu = mu2, log = TRUE) +
            log(pmax(1 + lam*(exp(-y1)-c1)*(exp(-y2)-c2), 1e-300))
  expect_equal(got, exp_ll, tolerance = 1e-10)
})

test_that("analytic gradient matches numDeriv", {
  set.seed(8)
  n  <- 40
  X1 <- cbind(1, rnorm(n)); X2 <- cbind(1, rnorm(n))
  y1 <- rpois(n, 2); y2 <- rpois(n, 3)
  par <- c(0.2, 0.1, 0.1, -0.05, log(0.5), log(0.5), 0.2)
  f  <- function(p) sum(rpbnb:::bnb_loglik_vec(p, y1, y2, X1, X2))
  num <- numDeriv::grad(f, par)
  ana <- rpbnb:::bnb_grad_vec(par, y1, y2, X1, X2)
  expect_equal(ana, num, tolerance = 1e-4)
})
```

- [ ] **Step 3: Run these two tests**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-fit-bnb.R')"`
Expected: both PASS.

- [ ] **Step 4: Commit**

```bash
git add R/bnb_likelihood.R tests/testthat/test-fit-bnb.R
git commit -m "feat: BNB likelihood internals with gradient check"
```

### Task 5b/5c: `fit_bnb()` wrapper + independence

- [ ] **Step 5: Write the failing test for `fit_bnb`**

Append to `tests/testthat/test-fit-bnb.R`:

```r
sample_data <- function() {
  read.csv(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb"))
}

test_that("fit_bnb famoye reproduces legacy bnbr_v2-4 estimates on rwm1984", {
  skip_if_not(file.exists(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb")))
  d <- sample_data()
  fit <- fit_bnb(docvis ~ outwork + kids, hospvis ~ outwork + kids,
                 data = d, dependence = "famoye")
  expect_s3_class(fit, "bnb_fit")
  cf <- coef(fit)
  expect_true(all(c("b1:(Intercept)", "b2:(Intercept)") %in% names(cf)))
  # Intercept for docvis is around log of its mean (~0.9-1.1); sanity, not exact
  expect_gt(cf[["b1:(Intercept)"]], 0)
  expect_true(is.finite(logLik(fit)))
  expect_equal(length(coef(fit)), 4 + 3)  # 2 intercepts+2 slopes + log_m1,log_m2,z_lambda
})

test_that("fit_bnb independence equals two univariate NB2 fits", {
  skip_if_not(file.exists(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb")))
  skip_if_not_installed("MASS")
  d <- sample_data()
  fit <- fit_bnb(docvis ~ outwork, hospvis ~ outwork, data = d,
                 dependence = "independence")
  expect_s3_class(fit, "bnb_fit")
  g1 <- MASS::glm.nb(docvis ~ outwork, data = d)
  expect_equal(unname(coef(fit)[["b1:(Intercept)"]]),
               unname(coef(g1)[["(Intercept)"]]), tolerance = 1e-3)
  expect_equal(unname(coef(fit)[["b1:outwork"]]),
               unname(coef(g1)[["outwork"]]), tolerance = 1e-3)
})

test_that("fit_bnb famoye log-lik >= independence log-lik on the same data", {
  skip_if_not(file.exists(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb")))
  d <- sample_data()
  fi <- fit_bnb(docvis ~ outwork, hospvis ~ outwork, data = d, dependence = "independence")
  ff <- fit_bnb(docvis ~ outwork, hospvis ~ outwork, data = d, dependence = "famoye")
  expect_gte(as.numeric(logLik(ff)) + 1e-6, as.numeric(logLik(fi)))
})

test_that("fit_bnb errors on negative counts and unknown columns", {
  d <- data.frame(y1 = c(1, -1, 2), y2 = c(0, 1, 2), x = rnorm(3))
  expect_error(fit_bnb(y1 ~ x, y2 ~ x, data = d, dependence = "famoye"), "non-negative")
})
```

- [ ] **Step 6: Run to verify failure**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-fit-bnb.R')"`
Expected: the four new tests FAIL — `could not find function "fit_bnb"`. (The data-dependent tests need Task 11's `inst/extdata`; until then they `skip`. Run again after Task 11.)

- [ ] **Step 7: Write `R/fit_bnb.R`**

Port the estimator body from `Rcodes/bnbr_v2-4.R` `bnbr_famoye_bfgs` (lines 304–480) into an internal `fit_bnb_famoye(Y1, Y2, X1, X2, cn1, cn2, start, control)` that returns the raw pieces (par_hat, vcov, se, ll trace, fitted means, bounds). Then write the public `fit_bnb()` that:
  1. validates inputs and dependence arg,
  2. builds `model.frame`/`model.matrix` (preserving the `bnbr_famoye_bfgs` data handling at lines 311–321, including the negative-count check at 315),
  3. dispatches to `fit_bnb_famoye` or `fit_bnb_independence`,
  4. wraps the result via `new_bnb_fit()`.

Independence path `fit_bnb_independence`: fit each margin with `MASS::glm.nb` (NB2), assemble a combined coefficient vector named `b1:*`, `b2:*`, `log_m1 = log(1/theta1)`, `log_m2 = log(1/theta2)`, and a fixed `z_lambda` corresponding to `lambda = 0` (store `lambda = 0`, set its SE to `NA`). logLik = sum of the two margin logLiks; vcov = block-diagonal of the two margin vcovs padded with zeros for the dispersion/lambda slots (document that cross-margin covariances are zero by construction).

`new_bnb_fit()` constructor (put near top of `fit_bnb.R`):

```r
new_bnb_fit <- function(coef, vcov, se, logLik, nobs, npar, dependence,
                        lambda, bounds, mu1, mu2, X1, X2, Y1, Y2,
                        formula_1, formula_2, ll_trace, convergence, call) {
  structure(
    list(coef = coef, vcov = vcov, se = se, logLik = logLik,
         nobs = nobs, npar = npar, dependence = dependence,
         lambda = lambda, bounds = bounds, mu1 = mu1, mu2 = mu2,
         X1 = X1, X2 = X2, Y1 = Y1, Y2 = Y2,
         formula_1 = formula_1, formula_2 = formula_2,
         ll_trace = ll_trace, convergence = convergence,
         AIC = -2 * logLik + 2 * npar, BIC = -2 * logLik + log(nobs) * npar,
         call = call),
    class = "bnb_fit"
  )
}
```

Public signature with full roxygen:

```r
#' Fit a bivariate negative binomial regression model
#'
#' @param formula_1,formula_2 Model formulas for the two count outcomes.
#' @param data A data frame.
#' @param dependence Dependence structure: "independence" (two univariate NB2
#'   margins) or "famoye" (Famoye/Sarmanov bivariate NB).
#' @param start Optional starting parameter vector.
#' @param control An [rpbnb_control()] object.
#' @return An object of class `bnb_fit`.
#' @export
#' @examples
#' d <- read.csv(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb"))
#' fit <- fit_bnb(docvis ~ outwork, hospvis ~ outwork, data = d,
#'                dependence = "famoye")
#' summary(fit)
fit_bnb <- function(formula_1, formula_2, data,
                    dependence = c("independence", "famoye"),
                    start = NULL, control = rpbnb_control()) {
  dependence <- match.arg(dependence)
  # ... (build matrices, negative-count check, dispatch, wrap) ...
}
```

Keep the analytic-gradient + frozen-bounds numeric-Hessian SE machinery from the source unchanged inside `fit_bnb_famoye`. Suppress all `cat()` console printing from the source (the `print_output` branch lines 455–461) — printing is the job of the `print.bnb_fit` method (Task 9).

- [ ] **Step 8: Run the gradient/likelihood tests (data tests still skip)**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-fit-bnb.R')"`
Expected: gradient/likelihood/negative-count tests PASS; rwm1984 tests SKIP (no extdata yet).

- [ ] **Step 9: Commit**

```bash
git add R/fit_bnb.R tests/testthat/test-fit-bnb.R
git commit -m "feat: fit_bnb with famoye and independence dependence"
```

---

## Task 6: RP-BNB likelihood internals + `fit_rpbnb()`

**Files:**
- Create: `R/rpbnb_likelihood.R` (internal simulated-LL + gradient + fixed-bounds)
- Create: `R/fit_rpbnb.R` (public `fit_rpbnb`, `rpbnb_fit` constructor)
- Test: `tests/testthat/test-fit-rpbnb.R`

### Task 6a: Internals

- [ ] **Step 1: Write `R/rpbnb_likelihood.R`**

Port verbatim from `Rcodes/rpbnbr_faymore.R`: `bnbr_rp_ll_and_grad` (lines 61–191, keep the `compiler::cmpfun` wrapper) and `bnbr_rp_ll_fixed_bounds` (194–243). Remove their local core-math redefinitions (use `famoye_core.R`). Parameter vector: `[beta1 (k1), beta2 (k2), log_sd1 (q1), log_sd2 (q2), log_m1, log_m2, z_lambda]`. Replace the internal `rowLogSumExp` call with `row_log_sum_exp`. Mark internal.

- [ ] **Step 2: Write the gradient-check test**

`tests/testthat/test-fit-rpbnb.R`:

```r
test_that("RP-BNB simulated-LL analytic gradient matches numDeriv", {
  set.seed(11)
  n  <- 30
  X1 <- cbind(1, rnorm(n)); X2 <- cbind(1, rnorm(n))
  y1 <- rpois(n, 2); y2 <- rpois(n, 2)
  XR1 <- X1[, 2, drop = FALSE]; XR2 <- X2[, 2, drop = FALSE]
  rand_idx1 <- 2L; rand_idx2 <- 2L
  Z <- rpbnb:::halton_normal(60, 2, burn = 50, skip = 10)
  Z1 <- Z[, 1, drop = FALSE]; Z2 <- Z[, 2, drop = FALSE]
  par <- c(0.2, 0.1, 0.15, -0.1, log(0.3), log(0.3), log(0.4), log(0.4), 0.1)

  obj <- function(p) as.numeric(
    rpbnb:::bnbr_rp_ll_and_grad(p, y1, y2, X1, X2, XR1, XR2,
                                rand_idx1, rand_idx2, Z1, Z2))
  v   <- rpbnb:::bnbr_rp_ll_and_grad(par, y1, y2, X1, X2, XR1, XR2,
                                     rand_idx1, rand_idx2, Z1, Z2)
  ana <- attr(v, "gradient")
  num <- numDeriv::grad(obj, par)
  expect_equal(ana, num, tolerance = 1e-3)
})
```

- [ ] **Step 3: Run to verify pass**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-fit-rpbnb.R')"`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add R/rpbnb_likelihood.R tests/testthat/test-fit-rpbnb.R
git commit -m "feat: RP-BNB simulated likelihood internals with gradient check"
```

### Task 6b: `fit_rpbnb()` wrapper

- [ ] **Step 5: Write the failing recovery test**

Append to `tests/testthat/test-fit-rpbnb.R`:

```r
test_that("fit_rpbnb recovers simulated parameters (small case)", {
  sim <- simulate_rpbnb(
    n = 1500,
    beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
    beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
    random_1 = list(x1 = list(sd = 0.5)),
    random_2 = NULL,
    dispersion = c(m1 = 0.4, m2 = 0.5),
    lambda = 0,
    seed = 99
  )
  fit <- fit_rpbnb(y1 ~ x1, y2 ~ x1, data = sim$data,
                   random_1 = "x1", draws = 200, seed = 123,
                   control = rpbnb_control(compute_se = FALSE))
  expect_s3_class(fit, "rpbnb_fit")
  cf <- coef(fit)
  expect_equal(unname(cf[["b1:(Intercept)"]]), 0.2, tolerance = 0.15)
  expect_equal(unname(cf[["b1:x1"]]),          0.4, tolerance = 0.15)
  # recovered sd shown on natural scale
  sd_hat <- unname(cf[["sd1:x1"]])
  expect_equal(sd_hat, 0.5, tolerance = 0.25)
})

test_that("fit_rpbnb with sd->0 approaches fixed-parameter BNB", {
  set.seed(3)
  sim <- simulate_rpbnb(
    n = 800,
    beta1 = c("(Intercept)" = 0.3, x1 = 0.2),
    beta2 = c("(Intercept)" = 0.2, x1 = 0.1),
    random_1 = list(x1 = list(sd = 1e-4)),
    dispersion = c(m1 = 0.4, m2 = 0.4), lambda = 0, seed = 5
  )
  rp <- fit_rpbnb(y1 ~ x1, y2 ~ x1, data = sim$data, random_1 = "x1",
                  draws = 150, seed = 1, control = rpbnb_control(compute_se = FALSE))
  bn <- fit_bnb(y1 ~ x1, y2 ~ x1, data = sim$data, dependence = "famoye")
  expect_equal(unname(coef(rp)[["b1:x1"]]),
               unname(coef(bn)[["b1:x1"]]), tolerance = 0.1)
})

test_that("fit_rpbnb errors on unknown random name", {
  sim <- simulate_rpbnb(n = 100, beta1 = c("(Intercept)" = 0.2, x1 = 0.3),
                        beta2 = c("(Intercept)" = 0.1, x1 = 0.1),
                        dispersion = c(m1 = 0.4, m2 = 0.4), seed = 1)
  expect_error(
    fit_rpbnb(y1 ~ x1, y2 ~ x1, data = sim$data, random_1 = "nope",
              draws = 50, control = rpbnb_control(compute_se = FALSE)),
    "nope"
  )
})
```

- [ ] **Step 6: Run to verify failure**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-fit-rpbnb.R')"`
Expected: FAIL — `could not find function "fit_rpbnb"` (and `simulate_rpbnb`, built in Task 7; reorder execution so Task 7 precedes Task 6b, or accept these tests stay red until Task 7 lands — they are committed here and verified green at the end of Task 7).

- [ ] **Step 7: Write `R/fit_rpbnb.R`**

Port the wrapper body from `Rcodes/rpbnbr_faymore.R` `rpbnbr_bfgs` (lines 246–486) into an internal core that takes pre-built `X1,X2,Y1,Y2` and the random indices, plus a public `fit_rpbnb()`. Required edits versus the source:
  - **Rename args** to the package API: `formula_1, formula_2, data, random_1 = NULL, random_2 = NULL, draws = 400, draw_type = "halton", seed = 1234, start = NULL, control = rpbnb_control()`. Map `random_1 → rand_names1`, `random_2 → rand_names2`, `draws → n_draws`, and pull `n_draws_hess, halton_burn, halton_skip, n_cores, compute_se, method, iterlim, reltol` from `control`.
  - **Seed:** call `set.seed(seed)` before `halton_normal` so draws are reproducible (the source relied on `usetime`; we use deterministic draws + seed per spec §3).
  - **draw_type:** `match.arg(draw_type, "halton")` — only Halton in Phase 1; the arg exists for forward-compatibility.
  - **Unknown random names:** the source silently drops names not in `colnames(X)` (lines 269–273). Change to `stop()` with the offending names (test "errors on unknown random name").
  - Replace `make_halton_norm` with `halton_normal`, `rowLogSumExp` with `row_log_sum_exp`.
  - Suppress `cat()` printing (source lines 452–470); defer to `print.rpbnb_fit`.
  - Wrap output via `new_rpbnb_fit()` (below), preserving `par_opt`, `vcov_opt`, `se`, the natural-scale display table, `m1/m2/lambda`, `bounds`, `ll_trace`, `X1/X2/Y1/Y2`, `rand_idx1/2`, plus metadata `draws, draw_type, seed`.

`new_rpbnb_fit()` constructor:

```r
new_rpbnb_fit <- function(coef, vcov, se, logLik, nobs, npar,
                          m1, m2, lambda, bounds, mu1, mu2, X1, X2, Y1, Y2,
                          rand_idx1, rand_idx2, formula_1, formula_2,
                          draws, draw_type, seed, ll_trace, convergence, call) {
  structure(
    list(coef = coef, vcov = vcov, se = se, logLik = logLik,
         nobs = nobs, npar = npar, m1 = m1, m2 = m2, lambda = lambda,
         bounds = bounds, mu1 = mu1, mu2 = mu2,
         X1 = X1, X2 = X2, Y1 = Y1, Y2 = Y2,
         rand_idx1 = rand_idx1, rand_idx2 = rand_idx2,
         formula_1 = formula_1, formula_2 = formula_2,
         draws = draws, draw_type = draw_type, seed = seed,
         ll_trace = ll_trace, convergence = convergence,
         AIC = -2 * logLik + 2 * npar, BIC = -2 * logLik + log(nobs) * npar,
         call = call),
    class = "rpbnb_fit"
  )
}
```

The `coef` vector for `rpbnb_fit` must use natural-scale, display-named entries (`b1:*`, `b2:*`, `sd1:*`, `sd2:*`, `m1`, `m2`, `lambda`) consistent with the source's `coef_table_display` (lines 408–439) so the recovery test's `cf[["sd1:x1"]]` works. Keep the raw estimation-scale vector in `fit$par_opt` and align `vcov` to it.

- [ ] **Step 8: Run (after Task 7 exists) to verify pass**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-fit-rpbnb.R')"`
Expected: all PASS once Task 7's `simulate_rpbnb` exists. (Recovery test runs ~seconds at n=1500, draws=200.)

- [ ] **Step 9: Commit**

```bash
git add R/fit_rpbnb.R
git commit -m "feat: fit_rpbnb bivariate random-parameter BNB (SIML)"
```

---

## Task 7: Simulator (`simulate_rpbnb`)

**Files:**
- Create: `R/simulate_rpbnb.R`
- Test: `tests/testthat/test-simulate-rpbnb.R`

- [ ] **Step 1: Write the failing test**

`tests/testthat/test-simulate-rpbnb.R`:

```r
test_that("simulate_rpbnb is reproducible for a fixed seed", {
  a <- simulate_rpbnb(n = 200, beta1 = c("(Intercept)" = 0.2, x1 = 0.3),
                      beta2 = c("(Intercept)" = 0.1, x1 = -0.2),
                      dispersion = c(m1 = 0.4, m2 = 0.5), seed = 42)
  b <- simulate_rpbnb(n = 200, beta1 = c("(Intercept)" = 0.2, x1 = 0.3),
                      beta2 = c("(Intercept)" = 0.1, x1 = -0.2),
                      dispersion = c(m1 = 0.4, m2 = 0.5), seed = 42)
  expect_identical(a$data, b$data)
})

test_that("simulate_rpbnb returns the documented pieces and true params", {
  s <- simulate_rpbnb(n = 100, beta1 = c("(Intercept)" = 0.2, x1 = 0.3),
                      beta2 = c("(Intercept)" = 0.1, x1 = -0.2),
                      random_1 = list(x1 = list(sd = 0.5)),
                      dispersion = c(m1 = 0.4, m2 = 0.5), lambda = 0, seed = 1)
  expect_true(all(c("data", "coef_realized", "mu", "true", "settings", "meta") %in% names(s)))
  expect_true(all(c("y1", "y2", "x1") %in% names(s$data)))
  expect_equal(s$true$dispersion[["m1"]], 0.4)
  expect_equal(s$true$random_1$x1$sd, 0.5)
  expect_equal(nrow(s$data), 100)
})

test_that("realized random coefficients match requested mean and sd", {
  s <- simulate_rpbnb(n = 20000, beta1 = c("(Intercept)" = 0.0, x1 = 0.5),
                      beta2 = c("(Intercept)" = 0.0, x1 = 0.0),
                      random_1 = list(x1 = list(sd = 0.7)),
                      dispersion = c(m1 = 0.4, m2 = 0.4), seed = 2)
  bx1 <- s$coef_realized$eq1[, "x1"]
  expect_equal(mean(bx1), 0.5, tolerance = 0.02)
  expect_equal(sd(bx1),  0.7, tolerance = 0.02)
})

test_that("counts are overdispersed when dispersion > 0", {
  s <- simulate_rpbnb(n = 5000, beta1 = c("(Intercept)" = 1.0, x1 = 0.0),
                      beta2 = c("(Intercept)" = 1.0, x1 = 0.0),
                      dispersion = c(m1 = 0.8, m2 = 0.8), seed = 3)
  expect_gt(var(s$data$y1), mean(s$data$y1))  # NB variance > mean
})
```

- [ ] **Step 2: Run to verify failure**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-simulate-rpbnb.R')"`
Expected: FAIL — `could not find function "simulate_rpbnb"`.

- [ ] **Step 3: Write `R/simulate_rpbnb.R`**

New code (no legacy source). Generates covariates (standard normal by default for each non-intercept name found across `beta1`/`beta2`), draws per-observation random coefficients `β_i = mean + sd·N(0,1)` for names listed in `random_1`/`random_2`, forms `μ1, μ2` via NB2 mean, then draws counts. To honor `lambda` (Famoye dependence) generate `y1 ~ NB2(μ1, m1)` then `y2` from the Famoye conditional; for Phase 1, if `lambda == 0` draw the two margins independently via `rnbinom(size = 1/m, mu = μ)`, and if `lambda != 0` use rejection sampling against the Famoye joint pmf on a truncated grid (port the conditional sampler idea from `Rcodes/BNBR_MLE.R` `rBNBR`/`rY2_given_Y1` if present; otherwise independent margins with a warning that non-zero lambda simulation is approximate). Default tests use `lambda = 0`.

Full signature + return contract:

```r
#' Simulate data from a random-parameter bivariate NB process
#'
#' @param n Number of observations.
#' @param beta1,beta2 Named numeric vectors of fixed coefficient means per
#'   equation; must include "(Intercept)".
#' @param random_1,random_2 Named lists giving random coefficients, e.g.
#'   `list(x1 = list(sd = 0.5))`. Means come from `beta1`/`beta2`.
#' @param dispersion Named numeric `c(m1 = ..., m2 = ...)` NB2 dispersions.
#' @param lambda Famoye dependence parameter (0 = independent margins).
#' @param covariates Optional data frame of covariates; if NULL, standard-normal
#'   columns are generated for every non-intercept name.
#' @param seed Random seed.
#' @return A list with `data` (y1, y2, covariates), `coef_realized`
#'   (per-obs coefficients per equation), `mu` (conditional means), `true`
#'   (parameters), `settings`, and `meta` (R/seed/timestamp passed by caller).
#' @export
#' @examples
#' sim <- simulate_rpbnb(n = 500,
#'   beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
#'   beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
#'   random_1 = list(x1 = list(sd = 0.5)),
#'   dispersion = c(m1 = 0.4, m2 = 0.5), seed = 1)
#' head(sim$data)
simulate_rpbnb <- function(n, beta1, beta2,
                           random_1 = NULL, random_2 = NULL,
                           dispersion = c(m1 = 0.5, m2 = 0.5),
                           lambda = 0, covariates = NULL, seed = 1234) {
  stopifnot("(Intercept)" %in% names(beta1), "(Intercept)" %in% names(beta2))
  if (!all(c("m1", "m2") %in% names(dispersion))) {
    stop("`dispersion` must be a named vector c(m1 = ., m2 = .).", call. = FALSE)
  }
  chk_rand <- function(rl, bv, lbl) {
    for (nm in names(rl)) if (!nm %in% names(bv)) {
      stop("random name '", nm, "' not in ", lbl, ".", call. = FALSE)
    }
  }
  chk_rand(random_1, beta1, "beta1"); chk_rand(random_2, beta2, "beta2")
  if (lambda != 0) {
    stop("Phase 1 simulate_rpbnb supports lambda = 0 (independent margins) only.",
         call. = FALSE)
  }

  set.seed(seed)
  vars <- setdiff(union(names(beta1), names(beta2)), "(Intercept)")
  if (is.null(covariates)) {
    covariates <- as.data.frame(stats::setNames(
      lapply(vars, function(v) rnorm(n)), vars))
  }
  build_X <- function(bv) {
    X <- cbind(`(Intercept)` = 1)
    for (nm in setdiff(names(bv), "(Intercept)")) X <- cbind(X, covariates[[nm]])
    colnames(X) <- names(bv)
    X
  }
  X1 <- build_X(beta1); X2 <- build_X(beta2)

  realize <- function(bv, rl, X) {
    B <- matrix(rep(bv, each = n), nrow = n, dimnames = list(NULL, names(bv)))
    for (nm in names(rl)) B[, nm] <- bv[[nm]] + rl[[nm]]$sd * rnorm(n)
    B
  }
  B1 <- realize(beta1, random_1, X1); B2 <- realize(beta2, random_2, X2)
  mu1 <- exp(rowSums(X1 * B1)); mu2 <- exp(rowSums(X2 * B2))
  m1 <- dispersion[["m1"]]; m2 <- dispersion[["m2"]]
  y1 <- rnbinom(n, size = 1 / m1, mu = mu1)
  y2 <- rnbinom(n, size = 1 / m2, mu = mu2)

  data <- data.frame(y1 = y1, y2 = y2, covariates)
  list(
    data = data,
    coef_realized = list(eq1 = B1, eq2 = B2),
    mu = data.frame(mu1 = mu1, mu2 = mu2),
    true = list(beta1 = beta1, beta2 = beta2, random_1 = random_1,
                random_2 = random_2, dispersion = dispersion, lambda = lambda),
    settings = list(n = n, seed = seed),
    meta = list(seed = seed, r_version = R.version.string)
  )
}
```

Validation is enforced above: `beta1`/`beta2` named with "(Intercept)"; every `random_*` name present in the corresponding `beta`; `dispersion` named `m1`,`m2`. `coef_realized$eq1` is an `n x length(beta1)` matrix with columns named like `beta1`.

- [ ] **Step 4: Run to verify pass**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-simulate-rpbnb.R')"`
Expected: PASS (4 tests).

- [ ] **Step 5: Run the RP-BNB recovery tests from Task 6b (now unblocked)**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-fit-rpbnb.R')"`
Expected: all PASS.

- [ ] **Step 6: Commit**

```bash
git add R/simulate_rpbnb.R tests/testthat/test-simulate-rpbnb.R
git commit -m "feat: simulate_rpbnb normal random-coefficient simulator"
```

---

## Task 8: S3 methods (`methods.R`)

**Files:**
- Create: `R/methods.R`
- Test: `tests/testthat/test-methods.R`

- [ ] **Step 1: Write the failing test**

`tests/testthat/test-methods.R`:

```r
make_small_bnb <- function() {
  set.seed(21)
  d <- data.frame(x = rnorm(400))
  d$y1 <- rnbinom(400, size = 2, mu = exp(0.3 + 0.2 * d$x))
  d$y2 <- rnbinom(400, size = 2, mu = exp(0.1 - 0.1 * d$x))
  fit_bnb(y1 ~ x, y2 ~ x, data = d, dependence = "famoye")
}

test_that("coef/vcov/logLik/AIC/BIC are consistent", {
  fit <- make_small_bnb()
  cf <- coef(fit); V <- vcov(fit)
  expect_true(is.numeric(cf) && length(cf) > 0)
  expect_equal(nrow(V), ncol(V))
  expect_s3_class(logLik(fit), "logLik")
  expect_equal(AIC(fit), -2 * as.numeric(logLik(fit)) + 2 * attr(logLik(fit), "df"))
  expect_true(is.finite(BIC(fit)))
})

test_that("predict returns one mean per row per outcome", {
  fit <- make_small_bnb()
  p <- predict(fit)
  expect_equal(nrow(p), fit$nobs)
  expect_true(all(c("mu1", "mu2") %in% names(p)))
  expect_true(all(p$mu1 > 0))
})

test_that("summary and print run without error", {
  fit <- make_small_bnb()
  expect_output(print(fit))
  s <- summary(fit)
  expect_output(print(s))
})
```

- [ ] **Step 2: Run to verify failure**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-methods.R')"`
Expected: FAIL — no `predict`/`summary` methods, default `coef` returns the list.

- [ ] **Step 3: Write `R/methods.R`**

Implement S3 methods for both `bnb_fit` and `rpbnb_fit`. `logLik.*` returns a `logLik` object with `df = npar`, `nobs = nobs`. `predict.*` returns `data.frame(mu1, mu2)` from stored `mu1/mu2` (recomputed from `newdata` when supplied via `model.matrix` against stored formulas). For `rpbnb_fit`, `predict` default `type = "response"` returns the unconditional mean stored in `mu1/mu2`. `print.*` shows the readable coefficient table; `summary.*` returns a `summary.bnb_fit`/`summary.rpbnb_fit` object with a coefficient matrix (Estimate/SE/z/p) and a `print` method using `signif_stars`.

```r
#' @export
coef.bnb_fit <- function(object, ...) object$coef
#' @export
vcov.bnb_fit <- function(object, ...) object$vcov
#' @export
logLik.bnb_fit <- function(object, ...) {
  structure(as.numeric(object$logLik), df = object$npar,
            nobs = object$nobs, class = "logLik")
}
#' @export
predict.bnb_fit <- function(object, newdata = NULL, ...) {
  if (is.null(newdata)) return(data.frame(mu1 = object$mu1, mu2 = object$mu2))
  X1 <- model.matrix(object$formula_1[-2], newdata)
  X2 <- model.matrix(object$formula_2[-2], newdata)
  b1 <- object$coef[grep("^b1:", names(object$coef))]
  b2 <- object$coef[grep("^b2:", names(object$coef))]
  data.frame(mu1 = as.vector(exp(X1 %*% b1)),
             mu2 = as.vector(exp(X2 %*% b2)))
}

# Shared builder: coefficient matrix Estimate/SE/z/p with stars.
.coef_matrix <- function(object) {
  est <- object$coef
  se  <- object$se[names(est)]
  z   <- est / se
  p   <- 2 * pnorm(-abs(z))
  data.frame(Parameter = names(est), Estimate = as.numeric(est),
             StdErr = as.numeric(se), z = as.numeric(z), p = as.numeric(p),
             Signif = signif_stars(p), row.names = NULL, check.names = FALSE)
}

#' @export
print.bnb_fit <- function(x, digits = 4, ...) {
  cat("Bivariate NB (", x$dependence, ") fit\n", sep = "")
  cat("Call: "); print(x$call)
  tab <- .coef_matrix(x)
  num <- vapply(tab, is.numeric, TRUE)
  tab[num] <- lapply(tab[num], formatC, format = "f", digits = digits)
  print(tab, row.names = FALSE, right = TRUE)
  cat(sprintf("\nlogLik = %.4f   AIC = %.4f   BIC = %.4f\n",
              as.numeric(x$logLik), x$AIC, x$BIC))
  cat("Signif: 0 '***' 0.001 '**' 0.01 '*' 0.05 '.' 0.1 ' ' 1\n")
  invisible(x)
}
#' @export
summary.bnb_fit <- function(object, ...) {
  structure(list(coefficients = .coef_matrix(object),
                 logLik = as.numeric(object$logLik), AIC = object$AIC,
                 BIC = object$BIC, nobs = object$nobs, npar = object$npar,
                 dependence = object$dependence, call = object$call),
            class = "summary.bnb_fit")
}
#' @export
print.summary.bnb_fit <- function(x, digits = 4, ...) {
  cat("Bivariate NB (", x$dependence, ") — summary\n", sep = "")
  tab <- x$coefficients
  num <- vapply(tab, is.numeric, TRUE)
  tab[num] <- lapply(tab[num], formatC, format = "f", digits = digits)
  print(tab, row.names = FALSE, right = TRUE)
  cat(sprintf("\nn = %d   k = %d   logLik = %.4f   AIC = %.4f   BIC = %.4f\n",
              x$nobs, x$npar, x$logLik, x$AIC, x$BIC))
  invisible(x)
}
```

Then add the `rpbnb_fit` analogues. Because `.coef_matrix`, `logLik`, `predict` only read generic fields (`coef`, `se`, `mu1`, `mu2`, `npar`, `nobs`) that both constructors populate identically, the `rpbnb_fit` methods can delegate:

```r
#' @export
coef.rpbnb_fit    <- function(object, ...) object$coef
#' @export
vcov.rpbnb_fit    <- function(object, ...) object$vcov
#' @export
logLik.rpbnb_fit  <- function(object, ...) logLik.bnb_fit(object, ...)
#' @export
predict.rpbnb_fit <- function(object, newdata = NULL, ...) predict.bnb_fit(object, newdata, ...)
#' @export
print.rpbnb_fit <- function(x, digits = 4, ...) {
  cat("Random-parameter bivariate NB fit (draws = ", x$draws,
      ", draw_type = ", x$draw_type, ")\n", sep = "")
  cat("Call: "); print(x$call)
  tab <- .coef_matrix(x)
  num <- vapply(tab, is.numeric, TRUE)
  tab[num] <- lapply(tab[num], formatC, format = "f", digits = digits)
  print(tab, row.names = FALSE, right = TRUE)
  cat(sprintf("\nlogLik = %.4f   AIC = %.4f   BIC = %.4f\n",
              as.numeric(x$logLik), x$AIC, x$BIC))
  invisible(x)
}
#' @export
summary.rpbnb_fit <- function(object, ...) {
  structure(list(coefficients = .coef_matrix(object),
                 logLik = as.numeric(object$logLik), AIC = object$AIC,
                 BIC = object$BIC, nobs = object$nobs, npar = object$npar,
                 draws = object$draws, call = object$call),
            class = "summary.rpbnb_fit")
}
#' @export
print.summary.rpbnb_fit <- function(x, digits = 4, ...) {
  cat("Random-parameter bivariate NB — summary (draws = ", x$draws, ")\n", sep = "")
  tab <- x$coefficients
  num <- vapply(tab, is.numeric, TRUE)
  tab[num] <- lapply(tab[num], formatC, format = "f", digits = digits)
  print(tab, row.names = FALSE, right = TRUE)
  cat(sprintf("\nn = %d   k = %d   logLik = %.4f   AIC = %.4f   BIC = %.4f\n",
              x$nobs, x$npar, x$logLik, x$AIC, x$BIC))
  invisible(x)
}
```

`AIC`/`BIC` work automatically through the `logLik` method (which carries `df` and `nobs`), so no custom `AIC.*`/`BIC.*` methods are needed — the test verifies this.

- [ ] **Step 4: Run to verify pass**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-methods.R')"`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add R/methods.R tests/testthat/test-methods.R
git commit -m "feat: S3 methods for bnb_fit and rpbnb_fit"
```

---

## Task 9: Diagnostics (`diagnostics.R`)

**Files:**
- Create: `R/diagnostics.R`
- Test: `tests/testthat/test-diagnostics.R`

- [ ] **Step 1: Write the failing test**

`tests/testthat/test-diagnostics.R`:

```r
make_diag_fit <- function() {
  set.seed(31)
  d <- data.frame(x = rnorm(500), z = rbinom(500, 1, 0.4))
  d$y1 <- rnbinom(500, size = 2, mu = exp(0.3 + 0.2 * d$x + 0.1 * d$z))
  d$y2 <- rnbinom(500, size = 2, mu = exp(0.1 - 0.1 * d$x))
  fit_bnb(y1 ~ x + z, y2 ~ x, data = d, dependence = "famoye")
}

test_that("bnb_gof returns finite AIC/BIC and pseudo-R2 in [0,1]", {
  g <- bnb_gof(make_diag_fit(), print_output = FALSE)
  expect_true(is.finite(g$AIC) && is.finite(g$BIC))
  expect_true(all(g$pseudoR2 >= 0 & g$pseudoR2 <= 1, na.rm = TRUE))
})

test_that("bnb_marginal_effects returns a row per requested variable", {
  me <- bnb_marginal_effects(make_diag_fit(), which = "y1", type = "AME",
                             print_output = FALSE)
  expect_true(all(c("x", "z") %in% me$Name))
  expect_true(all(is.finite(me$Estimate)))
})

test_that("bnb_elasticities runs for both margins", {
  el <- bnb_elasticities(make_diag_fit(), which = "both", type = "AME",
                         print_output = FALSE)
  expect_true(is.list(el) && all(c("y1", "y2") %in% names(el)))
})
```

- [ ] **Step 2: Run to verify failure**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-diagnostics.R')"`
Expected: FAIL — functions not found.

- [ ] **Step 3: Write `R/diagnostics.R`**

Port from `Rcodes/bnbr_v2-4.R`: `bnbr_gof` (73–151) → `bnb_gof`, `bnbr_me` (501–677) → `bnb_marginal_effects`, `bnbr_elasticities` (679–805) → `bnb_elasticities`. Required edits:
  - They read `fit$coef$beta1`, `fit$X1`, `fit$vcov`. The new `bnb_fit` stores a flat `coef` vector and `X1/X2/vcov`. Add small adapters at the top of each: extract `beta1 <- fit$coef[grep("^b1:", names(fit$coef))]` (strip the `b1:` prefix for names), `beta2` similarly, and `Vb1 <- fit$vcov[1:p1, 1:p1]`, `Vb2 <- fit$vcov[(p1+1):(p1+p2), ...]`. Because `bnb_fit$vcov` is ordered `[b1, b2, log_m1, log_m2, z_lambda]`, the beta blocks occupy the leading `p1 + p2` rows/cols — index accordingly.
  - `bnb_gof`'s null-model refit calls the old `bnbr_famoye_bfgs`; change it to call `fit_bnb(..., dependence = fit$dependence)` with intercept-only formulas built from the stored responses.
  - Add an `@export` and roxygen to each; add a `print_output = TRUE` arg already present in the source, default kept.
  - Replace `STAR_SYMS`/`symnum` blocks with `signif_stars()`.

Provide the `@param`/`@return` roxygen and keep the AME/MEM continuous-vs-binary auto-detection logic byte-identical (it is the validated part).

- [ ] **Step 4: Run to verify pass**

Run: `Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test-diagnostics.R')"`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add R/diagnostics.R tests/testthat/test-diagnostics.R
git commit -m "feat: GOF, marginal effects, elasticities for bnb_fit"
```

---

## Task 10: Package data, legacy preservation, docs, vignette

**Files:**
- Create: `inst/extdata/rwm1984_clean.csv` (copy)
- Create: `inst/legacy/` (copies of all `Rcodes/*.R` and `*.stan`)
- Create: `README.md`
- Create: `NEWS.md`
- Create: `vignettes/rpbnb-intro.Rmd`

- [ ] **Step 1: Copy the sample dataset into the package**

Run (PowerShell):
```powershell
New-Item -ItemType Directory -Force inst\extdata | Out-Null
Copy-Item data\rwm1984_clean.csv inst\extdata\rwm1984_clean.csv
```

- [ ] **Step 2: Preserve legacy scripts untouched**

Run (PowerShell):
```powershell
New-Item -ItemType Directory -Force inst\legacy | Out-Null
Copy-Item Rcodes\*.R   inst\legacy\ -Force
Copy-Item Rcodes\*.stan inst\legacy\ -Force
```

- [ ] **Step 3: Write `README.md`** (installation + quick start)

```markdown
# rpbnb

Bivariate and random-parameter negative binomial regression (NB2), via maximum
likelihood (Famoye/Sarmanov) and maximum simulated likelihood.

## Install

```r
# install.packages("devtools")
devtools::install("path/to/rpbnb")
```

## Quick start

```r
library(rpbnb)
d <- read.csv(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb"))

# Bivariate NB (Famoye dependence)
fit <- fit_bnb(docvis ~ outwork + kids, hospvis ~ outwork + kids,
               data = d, dependence = "famoye")
summary(fit)

# Random-parameter BNB
rp <- fit_rpbnb(docvis ~ outwork + kids, hospvis ~ outwork + kids,
                data = d, random_1 = "kids", draws = 400, seed = 1234)
summary(rp)
```

See `vignette("rpbnb-intro")`.
```

- [ ] **Step 4: Write `NEWS.md`**

```markdown
# rpbnb 0.1.0

* Initial release: `fit_bnb()` (independence, famoye), `fit_rpbnb()` (bivariate
  random-parameter BNB via simulated likelihood), `simulate_rpbnb()`,
  `rpbnb_control()`, S3 methods, and diagnostics ported and validated from the
  original `Rcodes` scripts.
```

- [ ] **Step 5: Write `vignettes/rpbnb-intro.Rmd`**

```
---
title: "Introduction to rpbnb"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Introduction to rpbnb}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>")
```

```{r setup}
library(rpbnb)
d <- read.csv(system.file("extdata", "rwm1984_clean.csv", package = "rpbnb"))
```

## Bivariate NB

```{r}
fit <- fit_bnb(docvis ~ outwork, hospvis ~ outwork, data = d,
               dependence = "famoye")
summary(fit)
bnb_gof(fit, print_output = FALSE)$AIC
```

## Random parameters

```{r}
rp <- fit_rpbnb(docvis ~ outwork + kids, hospvis ~ outwork, data = d,
                random_1 = "kids", draws = 200, seed = 1,
                control = rpbnb_control(compute_se = FALSE))
coef(rp)
```

## Simulation

```{r}
sim <- simulate_rpbnb(n = 1000,
  beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
  beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
  random_1 = list(x1 = list(sd = 0.5)),
  dispersion = c(m1 = 0.4, m2 = 0.5), seed = 1)
head(sim$data)
```
```

- [ ] **Step 6: Commit**

```bash
git add inst README.md NEWS.md vignettes
git commit -m "docs: sample data, legacy preservation, README, vignette"
```

---

## Task 11: Document, full test, and `R CMD check`

**Files:**
- Modify: `NAMESPACE`, `man/*` (generated)

- [ ] **Step 1: Generate documentation and NAMESPACE**

Run: `Rscript -e "devtools::document('.')"`
Expected: writes `man/*.Rd` and an updated `NAMESPACE` exporting `fit_bnb`, `fit_rpbnb`, `simulate_rpbnb`, `rpbnb_control`, `bnb_gof`, `bnb_marginal_effects`, `bnb_elasticities`, and the S3 methods. No roxygen errors.

- [ ] **Step 2: Run the full test suite**

Run: `Rscript -e "devtools::test('.')"`
Expected: all tests PASS (data-dependent `fit_bnb` tests now find `inst/extdata`).

- [ ] **Step 3: Run `R CMD check`**

Run: `Rscript -e "devtools::check('.', args = '--no-manual', error_on = 'warning')"`
Expected: `0 errors | 0 warnings | 0 notes` (a NOTE about package size from the bundled PDFs is acceptable only if `docs/` is build-ignored; confirm `.Rbuildignore` excludes `docs` and `Rcodes`). Fix any documented-arguments / undefined-global / example-runtime warnings inline.

- [ ] **Step 4: Confirm examples run**

Run: `Rscript -e "devtools::run_examples('.')"`
Expected: all `@examples` execute without error (keep example `draws` small).

- [ ] **Step 5: Commit**

```bash
git add NAMESPACE man
git commit -m "docs: roxygen man pages and NAMESPACE; R CMD check clean"
```

---

## Self-Review Notes (for the implementer)

- **Spec coverage:** Task 0 (skeleton, §6A), Task 1 (core math, §7.1/7.2), Tasks 5–6 (fit_bnb/fit_rpbnb, §3/§5), Task 6 independence (§BNB validation comparison), Task 7 (simulator, §Task 4), Task 8 (S3 methods, §6A.1/§6F.14), Task 9 (diagnostics), Task 10 (data/legacy/README/vignette, §4/§6A/§10), Task 11 (check, §6D.1/§6F). Deferred items are listed in the spec §2 and intentionally absent.
- **Execution ordering caveat:** Task 7 (`simulate_rpbnb`) must be implemented before Task 6b's recovery tests go green. Either implement Task 7 before Task 6b, or accept Task 6b's tests stay red until Task 7 (they are verified green at Task 7 Step 5). A subagent runner should do Task 5 → 7 → 6 if it prefers each task ending green.
- **Type consistency:** the flat `coef` naming (`b1:`, `b2:`, `sd1:`, `sd2:`, `m1`, `m2`, `lambda`) is used identically by `fit_rpbnb`, `methods.R`, and `diagnostics.R` adapters. `new_bnb_fit`/`new_rpbnb_fit` field names match every consumer.
- **No math drift:** ported functions are pinned by `dnbinom` and numDeriv gradient tests plus the legacy-reproduction test on rwm1984.
```
